# Implementation Research & Examples

_For each consolidated epic: recommended approach, real tools (with approximate pricing), real-world precedents, build-vs-buy, rough effort/cost, LTAC funding note, and risks. Figures are approximate and should be verified before budgeting._

**Date:** July 4, 2026


---

## Bulletproof hours & 'open now' single source of truth `P0` · `Cost: Low` · 🔒 · _enhances existing_

One owner-editable source of truth for hours, holiday closures, and today's status that also feeds Google Business Profile and the in-app kiosk, so a visitor never sees 'open' at a locked door. The app must show an 'open now, X-min walk from the ferry' state and hide or flag any datum it can't confirm is current.

**Requested by:** Rosa Delgado, Carol Whitfield, Hank Boyd, Sam Okafor, Meg Sorensen, Dr. Aisha Bello, Danny Cho

### Approach

I'm Rosa — I run a little counter-service spot two minutes off the walk-off ramp, and here's the thing that keeps me up: a boat lands, forty people walk up the hill, and if this app tells even five of them I'm "open" when my door's locked, they're mad at ME, not the app. So I don't want anything fancy. I want the app to only ever say "open" when it's actually true, and when it can't be sure, I'd rather it say nothing than guess. Good news — your own docs tell me the bones are already here. You've got structured `weeklyHours` per business, the `getOpenStatus()` math that computes open/closed in Pacific time, the live green "Open now" badge, the "verified [date]" stamp, and even the walk-minutes-from-the-ferry number on my listing. **Build on that. Do not rebuild it.** This epic is three additions to something that already mostly works.

**Addition one — the one-tap "not today" toggle.** My regular hours live in the portal already. What I need is a big dumb button on my phone: "Closed today," "Closing early (pick a time)," and for the gas dock and a couple others "Out of fuel." I tap it, and by the time the next boat lands the app has flipped me to closed — and it clears itself at midnight so I never have to remember to switch it back on, because I *will* forget. You literally already built this exact pattern for the ferry boarding-pass override: a single record stamped with the Pacific day, honored only while that day is still today, lapsing at the next midnight with no timers and no daylight-savings headaches. Copy that store almost verbatim into a `hours-override` store keyed to my listing, have `getOpenStatus()` check it first, and force the badge to "Closed — [reason] today" when it's set. That's the heart of the whole ask and it's a known, proven shape in your code.

**Addition two — holidays I can set once, in advance.** Separate from the same-day panic button, let me punch in "closed July 4, closed Thanksgiving" ahead of time as date-keyed exceptions on my hours record. On that calendar day the open-now math treats me as closed regardless of my weekly pattern. This is the one piece of genuinely new data (there's no holiday/closure field on the listing type today), but it's a small, well-bounded add.

**Addition three — never show a stale guess.** Right now the app shows my "verified [date]" as a friendly stamp, but nobody enforces it. I want a hard rule: if my structured hours are older than some window (say 60–90 days, your call), the app hides the green "Open now" badge and shows a plain "Hours unverified — call ahead · last checked [date]" instead of confidently rendering a guess. That's the acceptance criterion about hiding what you can't confirm, and it's a filter on the badge and the walk-time line, not a schema change. And the walk-minutes-from-ferry number only renders next to a badge the system actually trusts. **On the Google side:** your syndication doc is honest that the GBP write path is *planned but not wired* — there's zero adapter code today. Finishing it is real work gated on the Chamber's own API-access approval and the "Chamber-as-Manager" auth model, so treat GBP as a fast-follow behind a feature flag: ship the in-app single-source-of-truth first (that's what stops the locked-door problem), then push the same hours record out to GBP via a one-way write when access lands. The in-app kiosk already reads the same overlay store, so it's covered for free the moment the store is right.

**Build vs buy:** BUILD the in-app single source of truth; INTEGRATE (one-way write) to Google as a flagged fast-follow; do NOT deep-link out for the core status. The whole point of this P0 is that WE own the truth so a visitor never sees 'open' at a locked door — you cannot outsource that to a third party and still guarantee it. And you're not starting cold: structured hours, the open-now math, the verified-date stamp, the walk-time field, and (critically) the boarding-pass-override auto-expiry pattern all already ship in the repo, so the same-day toggle and holiday exceptions are extensions, not a new system. Google Business Profile is a genuine write integration but it's outbound-only and gated on Chamber API approval, so wire it behind a feature flag after the in-app path is solid — it makes the town's Google results match, but it is not what fixes the locked-door problem. The in-app kiosk needs nothing extra: it already reads the same overlay store, so a correct store is a correct kiosk.

**Rough effort:** Core in-app work (same-day auto-expiring override store + one-tap owner panel + holiday-date exceptions + staleness-hides-the-badge filter, all reusing existing stores/math): ~3-5 dev-days. Finishing the live GBP write path (adapter, Chamber-as-Manager OAuth, pending-edit read-back loop, feature flag, one-way sync on save): ~4-7 dev-days once the Chamber's API access is approved — call the GBP piece a fast-follow, not a blocker. Chamber-side (recruiting owners to add the Chamber as a GBP Manager) is ongoing, low-tech outreach. · **Rough cost:** One-time: developer time only (roughly a dev-week for the in-app core, another for GBP). Recurring: ~$0 net-new — it rides the existing ~$7/mo Render (or Vercel) hosting and ISR; GBP APIs and the DIY override store are free. No per-transaction or SaaS fees. Only cost inflation risk is if you build the optional POS pull, which pulls in a member's own Toast/Square subscription (not the Chamber's cost).

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing in-app hours stores + boarding-pass-override pattern (DIY, in-repo) | The same-day auto-expiring 'closed today / closed early / out of fuel' toggle and the holiday-closure exceptions. Clone src/lib/stores/boarding-pass-store.ts into a per-listing hours-override store (Pacific-day-stamped, lapses at midnight), extend getOpenStatus() to check overrides + holiday dates first, and add a staleness filter to open-badge.tsx. | $0 — it's your own code on your existing overlay/ISR seam. | This is the recommended core. The boarding-pass override already proves the Pacific-day auto-expiry with no timers or DST math. Zero new dependencies, works offline-first via the same overlay stores, propagates in ~60s via ISR like every other portal edit. |
| Google Business Profile APIs (Business Information API v1 + legacy v4) | The one-way outbound write of the canonical hours record to Google so a search-result 'open now' matches the app. regularHours/specialHours via a PATCH with updateMask; validateOnly for a dry run. | Free API. Requires Chamber's own verified GBP 60+ days old + an approved Basic API Access application (calendar-time gate, not a dollar cost). | Per SYNDICATION.md this is planned but NOT yet wired — no adapter exists. Use the Chamber-as-Manager auth model (one OAuth covers all listings). Gotchas: 10 edits/min/profile cap (batch a save into one patch), edits can land as pending moderation (build a read-back loop), new Manager has a ~7-day cooldown. Build behind a feature flag. |
| Toast / Square 'open now' via POS hours (deep-link / read-only, optional) | For the handful of members who already keep hours perfectly in a POS, optionally letting the POS be the truth so they never double-enter. Square has a Merchants/Locations API exposing business_hours. | Free API tier for reading own location hours; POS itself is the member's existing cost. | HONEST caveat: most Kingston members are tiny/phone-only/seasonal and do NOT run Toast or Square consistently, so this is a nice-to-have for a few, never the primary path. The portal must stay the canonical source; a POS pull is at most an optional pre-fill. Do not make members depend on a POS to have correct hours. |
| date-fns-tz (or rely on native Intl, which the repo already uses) | Any additional Pacific-time date math for holiday-date matching and midnight roll-over if you don't want to hand-roll it. | $0, open-source. | Your hours.ts already does correct TZ math with native Intl.DateTimeFormat and pacificDayString() — you likely need NOTHING new here. Only reach for a library if holiday-date comparisons get gnarly; otherwise keep the zero-dependency posture. |

### Real-world examples

- **Google Business Profile — 'Special hours' + real-time 'temporarily closed / hours might differ'** — The gold standard visitors already expect: owner sets holiday/special hours in advance and a same-day status; Google surfaces 'Hours might differ' and hides confident open/closed when it's unsure rather than guessing. This is exactly the behavior we're mirroring in-app and syndicating to. (https://support.google.com/business/answer/6303076)
- **Yelp — 'Special Hours' and 'Temporarily Closed' owner controls** — Lets an owner mark holiday hours and a temporary closure that overrides the weekly pattern, and shows a plain closed/updated state instead of a stale 'open'. Good precedent for a one-tap override that supersedes regular hours. (Note: Yelp has no write API for us — this is a UX precedent only, per our syndication doc.) (https://biz.yelp.com)
- **OpenStreetMap opening_hours spec + evaluator** — The reference open-source model for structured hours including 'PH off' (public-holiday closed) and time-limited exceptions, with a well-tested evaluator. A sanity-check for our holiday/exception data model and edge cases like split shifts and past-midnight closes we already handle. (https://wiki.openstreetmap.org/wiki/Key:opening_hours)
- **go.visitbainbridgeisland.org (the neighboring competitor tourism app)** — A live Kingston-adjacent tourism PWA with per-listing hours and open/closed state; useful as the direct comparable a Chamber board member will pull up. Validates that an 'open now' state is table-stakes for a WA ferry-town app and that our verified-only posture is a differentiator. (https://go.visitbainbridgeisland.org)

**LTAC funding:** Squarely LTAC lodging-tax fundable: this is tourism INFORMATION/PROMOTION — an accurate open-now directory and its syndication to Google are exactly the marketing-platform use RCW 67.28 allows, benefiting overnight and 50+-mile visitors. It stays clean because it channels no money to individual for-profits and takes no per-transaction cut; the moment any 'order/pay' feature attaches a commission that would be the risky part, but pure hours/status/syndication is not it.

**Risks:** Owner adoption and freshness — the whole model dies if members don't tap the button or keep hours current. The auto-expiring same-day toggle (self-clears at midnight) and the staleness rule (hide the badge past ~60-90 days rather than show a guess) are the mitigations; make the toggle a one-tap, phone-first, offline-tolerant control or tiny seasonal operators won't use it.; GBP write path is unbuilt and externally gated — no adapter code exists yet, and it needs the Chamber's own verified 60+-day GBP plus an approved Basic API Access application, a ~7-day new-Manager cooldown, a 10-edits/min cap, and a pending-moderation read-back loop. Don't let 'finish GBP' block shipping the in-app fix; flag it as a fast-follow.; Edge cases in the open-now math when overrides stack — a same-day 'closed early' override plus a split shift plus a past-midnight close plus a holiday exception must compose correctly and always fail safe to 'closed/unverified', never to a false 'open'. Reuse and unit-test the existing getOpenStatus() rather than forking it.; Two-way drift if GBP or a POS is ever allowed to write back — keep the portal record strictly canonical and GBP strictly outbound (one-way). A bidirectional sync (or trusting a POS as truth) reintroduces exactly the 'which source is right' drift this epic exists to kill.

---

## Ten-second, delegated, low-tech self-service portal `P0` · `Cost: Low` · 🔒 · _enhances existing_

A mobile-first owner portal simple enough that a tech-averse or 70-year-old user edits hours, status, menu, or listing in under two minutes with big buttons and no manual — and can delegate that access to a daughter, trusted employee, or rotating volunteer without IT setup. Fits service businesses (broker, clinic) not just restaurants.

**Requested by:** Rosa Delgado, Carol Whitfield, Hank Boyd, Sam Okafor, Meg Sorensen, Tom Iverson, Nadia Fischer, Linda Cho-Ramirez, Dr. Aisha Bello, Marcus Reed

### Approach

I'm Rosa — I run a little storefront on the strip, and I'm the one asking for this loudest, but honestly the person who needs it most isn't me: it's my mom, who covers the register on the days I drive to Poulsbo, and it's Carol at 71 who still writes her hours on an index card taped to the door. So build this as an **enhancement of the portal that already exists**, not a new app. You already have invite-based accounts, scrypt passwords, HMAC session cookies, and per-listing editing living in `src/lib/auth.ts` over the same JSON/DB overlay seam as everything else. The three things missing are: (1) owners can't hand out access themselves — only the Chamber can mint invites today (the invites route is `requireAdmin`-gated); (2) there's one flat `role` + `linkedIds[]` per person, so I can't give my daughter "edit hours only" without making her a full manager; and (3) every profile shape assumes a restaurant or storefront, so Sam the broker and Dr. Bello's clinic don't fit. Fix those three and you've delivered the whole epic cheaply, because the plumbing is done.

For **delegation**, add an owner-initiated invite: on my listing's edit page, a big "Add a helper" button that lets me create a scoped sub-account link tied to just my listing(s), with a role I pick (Full, or "Hours & status only"). Extend the existing `InviteCode`/`User` records with an optional `scope` (which fields the account may touch) and let a `business` owner — not just admin — mint invites for ids they already manage. **Don't make me think about passwords for other people.** Send the helper a magic link by email (you already plan to use Resend, free tier covers 3,000/mo — this is a handful of emails a season) so my mom taps a link and she's in; keep the existing scrypt password path as the fallback for anyone who wants one. Magic-link email beats SMS here: no per-message cost, works on any device, and NIST has deprecated SMS OTP anyway. Revoke has to be one tap and instant — a "Remove" button next to each helper that deletes the sub-account and kills the session. That single flow satisfies "grant and revoke in minutes without calling the Chamber," including the Farmers Market handing the weekly lineup to a rotating volunteer.

For the **service-business shape**, don't bolt "area served" onto the Restaurant type — add a lightweight `profileTemplate` discriminator (`storefront | food | service | market`) that swaps *which fields the form shows*. A service profile shows contact, area served ("Kingston + north Kitsap"), and a status line ("booking through August") instead of cuisine/price-level/menu. Same store, same overlay, same syndication feeds — just a different field set and a different card renderer. That keeps Marcus the broker and the clinic first-class without forking the data model.

Finally, the **ruthless mobile pass** is the actual product here, and it's mostly design, not code. Every core edit — hours, "closed today," status line, a menu item, the description — has to be reachable in one or two taps from a phone home screen, thumb-sized targets (44px+), no nested menus, no jargon, and it has to work on the ferry with two bars. Since it's already a PWA with offline plumbing on the roadmap, make edits queue-and-sync so a save on a dead signal doesn't vanish. The one screen that earns its keep: a giant **"Closed today / Open"** toggle and an **hours** editor a 70-year-old finishes without a manual. If Carol can flip "closed today" from the parking lot when the pipes freeze, you've won.

**Build vs buy:** Build in-app — this is the one epic you should absolutely not outsource. The delegation/scoping logic is a direct extension of `src/lib/auth.ts` (add optional scope + profileTemplate to the InviteCode/User records, relax the invite route so a business owner mints invites for ids they already hold, add a magic-link redeem path). Buying an auth SaaS (Auth0, Clerk, WorkOS) would add a recurring bill, a privacy dependency, and login friction that fights the whole 'dead-simple for a 70-year-old' goal — the opposite of what these ten owners asked for. The only thing worth pulling in is Resend, which is already your planned mailer, for the magic link. Deep-linking out to Google Business Profile for delegation is a fine complement (you syndicate there anyway) but can't be the answer, because the whole point is the owner edits their Visit Kingston listing without leaving the app or learning a second tool.

**Rough effort:** 2-3 dev-weeks total: ~4-6 dev-days for owner-minted scoped invites + magic-link redeem + revoke; ~2-3 dev-days for the profileTemplate discriminator and the service-business form/card; ~4-6 dev-days for the ruthless mobile-simplicity pass (big-button edit screens, 'closed today' toggle, offline queue-and-sync) which is mostly design/QA. The mobile pass is the part that's easy to under-scope. · **Rough cost:** One-time: the dev effort above (internal). Recurring: ~$0 — magic-link email fits Resend's free tier at Kingston's volume (a few dozen invites a season), and it rides the existing ~$7/mo Render hosting with no new service. Effectively no marginal run cost.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing in-app auth (src/lib/auth.ts) — extend it | Owner-minted scoped sub-account invites, per-listing role scoping, and the profileTemplate discriminator. This is the core of the epic and it's a modification of code you already run, over the same JSON/Postgres overlay seam. | $0 (already built; incremental dev only) | Free/DIY option and the recommended one. Add optional scope + profileTemplate to InviteCode/User; relax the invite route so a business owner can mint invites for linkedIds they already hold. No new dependency, no vendor, keeps everything privacy-first and self-hosted. |
| Resend (email API) | Magic-link login and 'you've been added as a helper' emails so owners never have to invent passwords for delegates. | Free tier ~3,000 emails/mo, 100/day (approx, 2026); paid from ~$20/mo if ever exceeded — you won't at Kingston's scale | Already the app's planned mail provider per project docs. Magic link = one tap, works for elderly delegates on any device, no SMS per-message cost, and avoids SMS OTP (NIST-deprecated). Sign the link with the existing HMAC secret; single-use, short TTL. |
| Passkeys / WebAuthn (browser-native) — optional future | Let an owner re-enter their own portal with Face ID / fingerprint instead of a password on repeat visits. | $0 (native to iOS/Android/Chrome/Safari; no vendor) | Nice-to-have, not required for v1. Great for the owner's own device once they're set up; keep magic-link + password as the recovery paths so nobody gets locked out. Adds real dev though — defer unless owners ask. |
| Twilio SMS (verify/notify) — paid alternative, honestly not recommended here | SMS one-time codes if you insist on phone-based login for delegates. | ~$0.0079 per SMS + ~$1.15/mo per US number (approx, 2026); A2P 10DLC registration required | Included for completeness. Skip it: per-message cost, US A2P registration paperwork, and SMS OTP is deprecated by NIST. Magic-link email does the same job free. |
| Yext / GBP delegated management — reference model, not a buy | Studying how mature platforms let an owner delegate listing edits by individual location and by role. | Yext enterprise pricing (approx $4/location/mo and up, opaque); GBP delegation is free | Don't integrate — you already syndicate to Google Business Profile per your plan. Borrow the UX: GBP's own 'add a manager, remove a manager per location' flow is the exact mental model Carol already half-knows. Match that simplicity in-app. |

### Real-world examples

- **Google Business Profile (delegated managers)** — An owner adds or removes a 'Manager' per location in a couple of taps, by email, with no help-desk ticket — the exact grant/revoke flow this epic wants, and a pattern many Kingston owners already recognize. (https://support.google.com/business/answer/3403100)
- **Toast / Square (staff roles & permissions)** — Restaurant owners hand scoped access to staff — 'can edit menu' vs 'manager' — from a phone, so a shift lead updates an item without touching payroll. Proves owner-delegated, role-scoped, mobile-first editing works for tiny, high-turnover teams. (https://www.toasttab.com)
- **Yext (per-location, per-role listing delegation)** — Lets an account add editors to a single listing or an entity group and set what each can change, syncing one source of truth outward — the enterprise version of exactly the scope model we need. (https://help.yext.com)
- **LocalHop / community event calendars for chambers** — Chamber-run calendars where individual member orgs and rotating volunteers self-manage their own events and listings without the chamber staff being a bottleneck — the volunteer-handoff case (Farmers Market weekly lineup) in the wild. (https://www.localhop.com)

**LTAC funding:** Clean LTAC fit. This is core tourism-promotion platform infrastructure — the system that keeps member hours, status, and listings accurate for visiting ferry riders — with no per-transaction fees and no money steered to any individual for-profit, so it sits squarely in the safe zone under RCW 67.28. Fund it as platform/operations, the same basis as the rest of the app.

**Risks:** Delegation is a security surface: a business owner minting invites for their own listings must be strictly scoped to linkedIds they already hold, or someone escalates to editing a neighbor's listing. Enforce canEdit on every mutation, make magic links single-use + short-TTL, and make revoke instantly kill the session — the current stateless HMAC cookie doesn't self-expire on revoke, so add a revocation check (e.g. drop the user record and verify the uid still exists on each request).; Under-scoping the mobile pass: the invite plumbing is the easy 30%; the 'a 70-year-old finishes in under two minutes with no manual' bar is the real work and the actual acceptance criterion. Budget design/usability time and test with actual older, tech-averse owners (Carol, Hank), not just on a developer's phone.; profileTemplate sprawl: if 'service business' becomes a dozen bespoke templates, the form logic and syndication mapping rot. Keep it to a small fixed set (storefront/food/service/market) and resist per-member customization.; Volunteer/family handoff means low-trust, high-turnover accounts touching live public data. Scope the market volunteer to 'this listing's events/lineup only,' log who changed what, and make sure a departing volunteer's access is trivially revoked so a cancelled account can't be reused.

---

## Post-once, publish-everywhere events (incl. recurring) `P0` · `Cost: Medium` · 🔒 · _enhances existing_

Enter an event one time and have it become the authoritative record that syndicates to the town calendar, Google Business Profile, and social channels, with recurring events (Trivia Tuesdays) as a single repeating entry. Kills the 'I type trivia night into four places' problem and stale, contradictory listings.

**Requested by:** Danny Cho, Nadia Fischer, Linda Cho-Ramirez, Marcus Reed, Priya Nair

### Approach

I'm Danny — I run trivia and live-music nights, and right now I type "Trivia Tuesdays, 7pm" into our Facebook page, our Google listing, the Chamber calendar, and then it's already wrong on one of them by the time summer hits. So hear me plainly: **do not build a new system. Finish the one you already have.** The app already has the event calendar, the per-listing JSON + iCal feeds (`/api/feeds/events`), the paste-in embed widget, and the honest `/portal/syndicate` hub with the GBP and Meta plans already researched. The gap the acceptance criteria describe — one repeating entry instead of 52 copies, and a real push to Google/social — is three specific enhancements bolted onto that, not a rebuild.

**First, recurrence, because that's the part that makes me want to throw my phone.** Today the seed data literally stores every Sunday market as its own row (`public-market-2026-07-05`, `-07-12`, `-07-19`…). That's the exact thing I refuse to do by hand for a year. Add an optional `recurrence` field to the `EventItem` model that stores a standard RRULE string (e.g. `FREQ=WEEKLY;BYDAY=TU;UNTIL=20260919`), keep the single record as the authoritative entry, and **expand occurrences at read time** using the battle-tested `rrule.js` library — in `getEvents()`, in the events page grouping, and in the deconfliction check (`eventsSharingDate`) so it sees the expanded dates. In the portal form I want three dumb controls: "Repeats: Never / Weekly / Monthly," a weekday picker, and an "until" date. When I cancel one week (holiday) I add an EXDATE; when I move the whole thing I edit one place. The outbound iCal emitter already hand-folds lines correctly — it just needs to emit the `RRULE` (and `EXDATE`) line per VEVENT instead of one VEVENT per week, which is what every calendar app expects anyway.

**Second, the actual "publish everywhere" push, done Chamber-side, not per-owner.** SYNDICATION.md already nailed the model: the Chamber applies once for Google Business Profile API access and each of us just adds the Chamber's Google account as a **Manager** of our profile — one Chamber login covers all of us, no OAuth ceremony for me. Build the GBP write adapter behind a feature flag: hours via Business Information API v1, and events/posts via the v4 `localPosts` endpoint, which now supports **recurring posts natively** (weekly/monthly `RecurrenceInfo`) — so my Trivia Tuesday maps to one recurring GBP post, not a manual weekly grind. The non-negotiable engineering detail is the **pending-edit read-back loop** (`hasPendingEdits`): if Google silently queues my edit in moderation and nobody checks, the app is quietly lying about "post once, everywhere," and that's worse than doing nothing. Social goes exactly as the doc says — a Meta **pilot with ≤50 tester businesses** (Facebook feed posts, since there's no Facebook Events API, plus Instagram), measure whether owners like me actually want auto-posting before anyone signs up for the multi-week Advanced Access review. Until an adapter is live for a given channel, the honest `/portal/syndicate` copy-and-deep-link path stays the answer — never flip that "no auto-sync yet" callout before the code ships.

**Third, let the rest of us in.** Right now events post through business or nonprofit portals; the realtor doing an open house and the pharmacy running a flu-clinic day (Marcus, Priya) have nowhere to go. Add a lightweight "community poster" role/flow that writes the same `EventItem` to the same calendar and map, with light admin moderation so it doesn't turn into spam. Same authoritative record, same feeds, same map pin — just more of us allowed to feed it.

**Build vs buy:** BUILD the recurrence model in-app; INTEGRATE (Chamber-side) for the outbound push; DEEP-LINK OUT as the honest fallback until each adapter is live. Recurrence and self-service community posting belong in the app because they're small enhancements to code that already exists (the EventItem model, the feeds, the portal form, the map) and because the calendar being the authoritative single source is the whole point — you can't outsource that. The Google/social push should be integration against Google Business Profile and Meta using the Chamber-as-Manager / pilot-tester model already researched in SYNDICATION.md, not a bespoke build and not a paid aggregator by default (an aggregator re-introduces the drift and vendor cost we're trying to kill). Everywhere an adapter isn't live yet, keep the existing copy-paste + deep-link path and its honest 'no auto-sync yet' labeling — deep-linking out is the correct, non-deceptive default, and it's what protects owners like me from a listing that silently goes stale.

**Rough effort:** Recurrence (model + rrule.js expansion in getEvents/events page/deconfliction, portal form controls, iCal RRULE/EXDATE output): ~1-1.5 dev-weeks. GBP write adapter behind a flag + the hasPendingEdits read-back loop + a 2-3 business pilot: ~1.5-2 dev-weeks of engineering, but gated on weeks of Chamber calendar time for GBP API-access approval and Manager cooldowns. Meta pilot (Facebook feed + IG, ≤50 testers, posting-failure alerts): ~1 dev-week. Community-poster role + moderation: ~3-5 dev-days. Total build: ~4-6 dev-weeks phased; the syndication legs are calendar-time-gated, not effort-gated. · **Rough cost:** One-time: the dev effort above; ~$0 in new licenses (rrule.js free, GBP + Meta APIs free). Recurring: effectively $0 added — stays within the existing ~$0-8/mo hosting; the only ongoing cost is human (Chamber maintaining the GBP-Manager relationships and watching posting-failure alerts). If the Chamber instead buys a social scheduler as a stopgap, budget ~$6-30/mo (approx) for Buffer/Zapier, but that's optional and I'd skip it in favor of the free GBP path.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| rrule.js (jkbrzt/rrule, npm) | Model and expand recurring events from a standard RRULE string (weekly/monthly, BYDAY, UNTIL, EXDATE). Used at read time to turn one 'Trivia Tuesdays' record into occurrences on the calendar, map, and iCal feed. | Free, open-source (MIT). ~1 small dependency. | The de-facto standard JS recurrence library, a port of Python's dateutil. Handles the ugly edge cases (DST, month-end, exceptions) I should not hand-roll. Alternatives: rSchedule or simple-rrule, but rrule.js is the safest, most-used pick. |
| Google Business Profile APIs (Business Information API v1 + v4 localPosts) | The write path so a single app entry pushes my hours + event/post to my Google listing, with native recurring-post support so weekly events don't need weekly re-posting. | Free API. Cost is calendar time: Chamber must have its own verified GBP 60+ days old and get approved for API access. | Use Chamber-as-Manager so one Chamber OAuth covers every listing. Gotchas per SYNDICATION.md: 10 edits/min/profile cap (batch each save), edits can land in moderation (build the hasPendingEdits read-back loop), ~7-day cooldown on a newly added Manager. No per-transaction fees — pure promotion, clean LTAC fit. |
| Meta Graph API (Facebook Pages + Instagram Content Publishing) | Push an event announcement to a business's Facebook Page feed and Instagram as part of the same post-once action. | Free API. Pilot needs no app review for up to ~50 tester businesses; scaling needs Business Verification + per-permission Advanced Access review (multi-week). | Honest limits: NO Facebook Events API — events go out as feed posts only. IG is JPEG-only, ~100 posts/day. Long-lived Page tokens don't expire but DIE on password change, so build posting-failure alerts. Pilot-first is the correct order. |
| The Events Calendar (Tribe) REST API + GrowthZone iCal — as INBOUND sources | Optional dedupe/ingest so the town calendar stays the authoritative superset by pulling the Chamber's own GrowthZone calendar and the Port of Kingston / explorekingstonwa.com Tribe feeds, rather than double-entry. | Free. Tribe REST (/wp-json/tribe/events/v1/) is already live and Chamber-controlled; GrowthZone exposes iCal. | This is the reverse direction from the epic (inbound, not outbound), but it's what makes 'the app is the authoritative source' true without re-typing what's already on those calendars. Parse VTIMEZONE properly and expand RRULEs; dedupe on normalized title + start date, prefer the Chamber record. |
| Buffer / Zapier (paid social-scheduler fallback) | A buy-instead-of-build shortcut for the social-push leg if the Chamber wants multi-channel posting before investing dev time in the Meta adapter and its review gauntlet. | Buffer ~$6/channel/mo (approx); Zapier ~$20-30/mo (approx) for multi-step Zaps. | Honest tradeoff: cheap to start but it's a per-Chamber account, not true per-member auto-posting, and it adds a vendor + a place for listings to drift. Fine as a stopgap; the GBP-Manager + Meta-pilot path is the real 'post once' answer and keeps everything inside the app the Chamber owns. |

### Real-world examples

- **The Events Calendar (Modern Tribe) — WordPress plugin powering explorekingstonwa.com and Port of Kingston** — Single recurring-event entry expanded into occurrences, with a machine-readable REST + iCal feed. It's literally the model for 'one entry, many occurrences, one feed' and two Kingston-relevant sites already run it, so it's both a precedent and a candidate ingest source. (https://theeventscalendar.com)
- **Google Business Profile — recurring Local Posts** — Lets a business (or its managing agency) publish one recurring post/event that keeps the listing fresh automatically instead of re-posting weekly — the exact 'Trivia Tuesdays as one repeating entry' behavior, native on the platform we most want to reach. (https://developers.google.com/my-business/content/posts-data)
- **Localist (used by universities and larger cities/DMOs for community calendars)** — One authoritative calendar with self-service community submission + moderation, recurring events, and syndication out to embeds/feeds and social. Good precedent for letting non-restaurant members (open houses, clinics) post into the same calendar without chaos. (https://www.localist.com)
- **Visit Bainbridge Island (nearby competitor DMO)** — Runs a curated town events calendar tied to local tourism promotion — the direct regional comparison the Chamber should at least match, and a reminder that our post-once + recurring + map-pin combo is a real differentiator if we ship it. (https://www.visitbainbridge.com)

**LTAC funding:** Strong LTAC fit: this is pure tourism promotion — a public events calendar and its syndication to Google and social — with no per-transaction fees flowing to any private for-profit, so the whole platform/marketing build is comfortably lodging-tax fundable under RCW 67.28. The only thing to keep clean is scope: fund the calendar/recurrence/syndication infrastructure and Chamber-wide promotion, not marketing spend earmarked for one specific business.

**Risks:** Silent syndication failure is the worst outcome: if a GBP edit sits in moderation (hasPendingEdits) or a Meta Page token dies on a password change and nobody's alerted, the app quietly lies about 'post once, everywhere' — which erodes trust faster than having no auto-sync. The read-back loop and posting-failure alerts are non-negotiable, not nice-to-haves.; Recurrence edge cases bite: canceling or moving a single occurrence (holiday week, one-off venue change) via EXDATE/overrides, DST boundaries, and 'edit this one vs. all future' semantics are exactly where hand-rolled logic breaks — lean on rrule.js and test the deconfliction/dedupe against expanded occurrences, not the single stored record.; Platform gates are calendar-time, not code: GBP API access needs the Chamber's own verified 60+-day-old profile and an approval wait, a newly added Manager faces a ~7-day cooldown, and Meta's scale phase is a multi-week per-permission review. Set owner expectations that auto-push lands in phases; don't flip the 'no auto-sync yet' copy early.; Self-service community posting (open houses, clinics) can invite spam or off-brand/borderline-commercial listings onto a tourism calendar; needs light admin moderation and clear rules, and note that non-tourism promotional posts could muddy the LTAC 'tourism promotion' justification if left ungoverned.

---

## Ferry-timed foot-traffic capture & staffing heads-up `P0` · `Cost: Medium` · _enhances existing_

Use the live WSF feed and existing busyness forecast to (a) give owners a ~10-minute heads-up before a busy boat unloads so they can staff up, and (b) surface to just-arrived walk-ons which nearby spots are open right now and worth the walk in their 12-minute window. Turns unpredictable bursts into measurable walk-ins.

**Requested by:** Rosa Delgado, Danny Cho, Sam Okafor, Carol Whitfield, Meg Sorensen, Tom Iverson, Marcus Reed

### Approach

This is squarely an *enhancement* of infrastructure the app already owns, not a new build. The live WSF feed is wired (`src/lib/wsf.ts` → `getVesselLocations()`, `getFerryStatusSnapshot()`), the per-sailing busyness model exists (`src/lib/ferry-forecast.ts`, plus the empirical-refinement layer that learns from every logged sailing), "near me" open-now discovery already runs on `/eat`, and per-sailing ferry reminders already fire a browser `Notification` (`src/lib/ferry-reminder.ts`). The two genuinely new pieces are (1) an **owner-facing staffing heads-up** and (2) a **just-arrived "what's open right now, and can I make it back" visitor surface**. Both should reuse the ferry snapshot rather than open any new data source.

**The precise timing signal is the thing to lean on.** WSF's Vessels API returns a live `Eta` (and `EtaBasis` / `LeftDock` / `AtDock`) per vessel — that's the real "boat lands in ~N minutes" clock, far better than a static schedule. Build one small server module that joins the inbound vessel's `Eta` to that sailing's forecast level, exposes it on the existing snapshot, and drives *both* audiences from it. For the **owner heads-up**, add a per-listing opt-in in the business portal (a toggle + a configurable lead time, default 10 min, and a "only when the boat is forecast BUSY" threshold so nobody gets pinged for a near-empty evening boat). Ship it first as a **browser push notification** (same `Notification` path the rider reminder already uses) plus a dead-simple "arrivals board" the owner can leave open on a back-office tablet: the next few inbound boats, ETA countdown, forecast level, and open drive-up spaces. Add SMS only as a paid upgrade for owners who won't keep a tab open (see tools). For the **visitor surface**, extend the arrived-walk-on view: on landing (or when a rider taps their reminder), show open-now spots sorted by walk-time-from-dock, each tagged "~4 min walk · get back before your 3:45 return" using the return sailing already in the schedule feed.

**Do not ship the staffing alert on the current model.** The forecast is admin-gated OFF precisely because the backtest shows ~+22 bias — it over-predicts busy. An over-predicting model that cries "staff up!" for empty boats will burn owner trust in one bad week and they'll turn it off forever. Gate the owner heads-up behind the *same* accuracy flag, and add an owner-side calibration guardrail: only fire above a level the backtest supports, and let each owner tune their own threshold and lead time. The visitor "what's open now" surface has no such risk — it's just discovery keyed to arrival time — so **it can ship immediately, decoupled from the forecast gate**, and start delivering value (and walk-in attribution) while the model is validated against more logged sailings.

**Keep it cheap, offline-tolerant, and low-touch.** The vessel ETA already refreshes on the existing polling cadence; the arrivals board is a thin client view over the snapshot with a service-worker cache so it survives a flaky back-room signal (last-known ETAs with a "stale" chip). Attribution — "boat landed → walk-ins" — rides the privacy-first analytics already in place (coarse, opt-in, no hoarding); tie the existing near-me GPS pings and QR/menu views to the arrival timestamp to produce the "measurable walk-ins" the epic promises, without adding any new tracking.

**Build vs buy:** Build in-app, reusing what exists — do not integrate a third-party foot-traffic or staffing product. Every input (WSF Eta, forecast, open-now hours, near-me, reminders, privacy analytics) is already owned by the app, so the marginal work is a thin timing module plus two views, not a new system. The only 'buy' is an optional SMS channel (Twilio/SNS) for owners who won't keep a PWA/tab open — deep-integrate that per-message rather than routing the whole feature through it. Avoid managed push (OneSignal) unless self-hosted web-push proves hard to operate; DIY web-push keeps device tokens in-house and matches the app's privacy-first posture. Net: build the timing + visitor surface now (independent of the forecast gate), build the owner heads-up behind the accuracy flag, and treat SMS as a paid opt-in add-on.

**Rough effort:** Visitor 'what's open now / make it back' surface: ~3-5 dev-days (reuses near-me + schedule). Owner arrivals board + per-listing opt-in/threshold/lead-time in the portal: ~4-6 dev-days. Web-push plumbing (VAPID + service worker, shared with the PWA milestone): ~3-5 dev-days. Optional SMS channel incl. A2P 10DLC setup: ~2-3 dev-days. Total roughly 2-3 dev-weeks for the free-tier version; SMS and any forecast re-calibration are additive. Model validation before enabling staffing alerts is ongoing (data accrues from logged sailings). · **Rough cost:** One-time: the 2-3 dev-weeks of build above; ~$0 in new services for the free-tier (web-push) version. Recurring: ~$0 incremental on the existing ~$7.25/mo Render host (WSF API and web push are free). If SMS is added: approx $1-2/mo per number plus ~$0.008-0.01 per text — realistically single-digit to low-tens of dollars/month per opted-in owner in peak season — plus a modest one-time A2P 10DLC registration. Push-only path stays at ~$0 marginal.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| WSDOT WSF Vessels + Terminals + Schedule API | The live timing + capacity signal: vessellocations Eta/LeftDock/AtDock for the ~10-min landing countdown, terminal drive-up space for real fullness, schedule for the return sailing used in 'get back before your return ferry'. Already integrated in wsf.ts. | Free (public gov API; requires a registered access code, already held: WSDOT_API_KEY). | Eta is the single most valuable field here and is already reachable via getVesselLocations(). No new dependency. Terminal drive-up space is the ground-truth capacity check the over-predicting forecast should defer to. |
| Web Push (VAPID) via web-push npm + service worker | Free owner + visitor push notifications for the staffing heads-up and return-ferry nudge, even when the tab is closed (PWA). | Free / DIY (self-hosted VAPID keys; ~$0). No third-party. | The rider reminder already uses in-page Notification; web push is the documented next step at the PWA milestone. This is the recommended default for low/no cost. iOS Safari needs the user to 'Add to Home Screen' first — a real limitation for walk-on visitors, less so for an owner's back-office tablet. |
| Twilio (or Amazon SNS) SMS | Optional paid upgrade: text the owner 'Busy boat lands in 10 min' when they won't keep a tab/PWA open — the most reliable channel for a low-tech, phone-in-pocket owner. | Approx $0.0079–$0.01 per SMS + ~$1.15/mo per number (Twilio, approx); a busy-season owner might get a few texts/day → single-digit dollars/mo. SNS is cheaper per message but less turnkey. | Honest caveat: A2P 10DLC registration is now required for US business SMS and adds setup friction/small fees. Only worth it for owners who ask; keep push as the free default. No commission model — it's flat per-message. |
| Existing in-app near-me + open-now discovery (/eat, map, ferry-status snapshot) | The visitor 'what's open right now, worth the walk' surface — reuse the verified open-now hours, walk-time-from-dock, and privacy-first geo already built. | Free (already built; ~$0 incremental on the current ~$7.25/mo Render host). | Build-in-app, not integrate. This is the half that ships now, independent of the forecast gate. Attribution rides existing coarse/opt-in analytics — no new tracking. |
| OneSignal (managed push, optional) | If self-hosted web-push proves fiddly to operate, a managed push service with a dashboard the Chamber can run without a dev. | Free tier covers web push for this scale; paid tiers ~$9+/mo only if you need advanced features. | Adds a third-party and a bit of privacy surface (device tokens) — weigh against the DIY web-push path, which keeps data in-house and matches the app's privacy posture. Prefer DIY unless operability becomes a problem. |

### Real-world examples

- **Disney / theme-park 'wait times + walk time' apps** — Surface live wait/arrival times and steer guests to what's worth doing in the window they have — the exact 'you have ~12 minutes, here's what's open and close' pattern, applied to a ferry burst instead of a ride queue. (https://disneyworld.disney.go.com)
- **Toast / Square for Restaurants 'rush' and staffing forecasts** — POS platforms that forecast busy periods and prompt operators to staff up; validates the owner-facing 'heads-up before a rush' behavior — the honest lesson is they earn trust only because the forecast is calibrated, which is why the WSF version must clear its backtest first. (https://pos.toasttab.com)
- **Visit Bainbridge Island app (direct competitor, same route family)** — A Lovable/Supabase tourism PWA that already does return-ferry alerts and QR-scan attribution for walk-on ferry riders — the closest real precedent for 'ferry-timed foot-traffic capture' on Puget Sound, and a source of ideas already flagged to steal (return-ferry alerts, QR attribution). (https://go.visitbainbridgeisland.org)
- **Transit / ferry ETA apps (WSDOT VesselWatch, Citymapper 'get off now')** — Real-time vessel/vehicle ETA driving a timed nudge to the user — the technical precedent for using WSF Eta to fire a countdown for both owners and arriving riders. (https://wsdot.com/ferries/vesselwatch/)

**LTAC funding:** The visitor-facing half (surfacing open-now spots and return-ferry timing to arriving tourists) is clean tourism promotion / visitor information and is comfortably LTAC lodging-tax fundable as part of the marketing/information platform. The owner staffing heads-up is a general platform/operations feature the members share, so it's defensible as platform cost — but keep it framed as information, not as a paid service channeling per-transaction revenue to individual for-profits, and don't fund any per-message SMS reselling or per-transaction fee out of LTAC.

**Risks:** Model over-prediction (~+22 bias) is the headline risk: shipping staffing alerts on the current forecast would cry 'staff up' for empty boats and destroy owner trust in a season. Keep the owner heads-up behind the existing accuracy gate; defer to live terminal drive-up space and require the backtest to clear before enabling.; Notification reliability for walk-on visitors: iOS Safari web push requires 'Add to Home Screen' first, which most transient ferry riders won't do — so the visitor 'get back in time' nudge must degrade to an in-page banner/countdown, not depend on push. SMS solves it for owners but adds A2P 10DLC friction and per-message cost.; A single substitute/smaller vessel or a mid-day schedule change can invalidate both the countdown and the capacity read; the live board must remain the authority and every surface must be labeled an estimate, with a 'stale' chip when the back-room signal drops.; Owner alert fatigue and staffing whiplash: a 10-min heads-up may be too short for a tiny owner-operated shop to actually add staff, turning the alert into noise. Make lead time and busy-threshold per-owner configurable, default conservative, and pair with the day-ahead ferry-vs-rush view so owners plan bakes/shifts in advance rather than reacting boat-by-boat.

---

## Up-the-hill discovery & fair featured placement `P0` · `Cost: Medium` · 🔒 · _enhances existing_

Make off-the-strip and up-the-hill businesses discoverable to walk-ons who assume 'everything is at the dock,' via open-now + walk-time discovery, fair rotating/featured placement, and active 'explore beyond the dock' nudges with quick loops. Drives physical visits, never an online cart.

**Requested by:** Carol Whitfield, Rosa Delgado, Hank Boyd, Danny Cho, Marcus Reed

### Approach

I run a shop up the hill, past Grocery Outlet, and I'll be blunt: every summer I watch a boatload of people walk off, buy a crêpe fifty feet from the ramp, take a photo of the marina, and get right back on. They think Kingston *is* the dock. It isn't. There's a whole second half of town they never learn exists, and the app is the only thing that can tell them in the ninety seconds they're deciding whether to wander. So build this on top of what you already have — the `/eat` page already sorts us into a "near-ferry" band and an "Up the hill" band by real walk time, and the "near me" button already lists what's open closest first. That bones is right. The problem is it only knows about *restaurants*. My gift shop, Rosa's gallery, Hank's hardware corner, the tasting rooms — we're not in that data at all except as dumb pins on the map. **Step one is a general "places" listing type**, not just a restaurant table: give a shop, gallery, winery, or service the same fields a restaurant gets — `lat`/`lng`, `walkMinutesFromFerry`, `weeklyHours` (so we get the same "Open now" badge), a category, and a one-line pitch. That's the same `Restaurant`-shaped record you already have in `src/lib/types.ts` and the same self-service portal editor, just generalized. Do that and the up-the-hill band fills up with the businesses that actually need it.

**Keep the walk time cheap and offline.** Don't go buy a routing API for this. You already fake walk minutes with a calibrated street-distance estimate from the walk-off point (47.7966, -122.4958), and honestly that's *fine* — the town is flat and small and nobody's going to sue you because it said 9 minutes and it was 11. Precompute the number once when a listing is saved (or seed it by hand like you do now) and store it on the record, so the phone does zero math and it all works with no signal in the ferry line. If the Chamber ever wants a real "everything reachable in 12 minutes" isochrone drawn on the map, OpenRouteService has a free tier (~2,500 calls/day) or you self-host Valhalla — but that's a nice-to-have, not launch. **The "explore beyond the dock" nudge is where you'll actually move my foot traffic**, and you already own the two things it needs: the ferry prediction stack knows when the next boat leaves, and Hunts/Itineraries are literally a "quick loop" primitive already. So compute `minutesUntilNextDeparture` (you have it), and when a walk-on has, say, 35+ minutes of slack, surface a card: *"You've got 40 min before the 2:30 boat — here's a 20-minute up-the-hill loop past three open shops."* Make it a tiny curated Itinerary/Hunt that only shows businesses currently open. That's the whole ballgame for me.

**On fairness — this is the part I care about most, so please don't screw it up.** Do NOT auction the featured slot. If it goes to the highest bidder, the three biggest members on the strip buy it every peak weekend and the rest of us are invisible forever — which is the exact problem this epic is supposed to *fix*. Do it as a **Chamber-curated deterministic rotation**: a seeded, date-based shuffle so every member gets the "Featured up-the-hill" spot an equal number of days, weighted if you want toward whoever's actually open right now and toward off-strip members (that's the point of the epic). Make it deterministic from the date so it's the same for every visitor that day, needs no cron, no ad server, no per-user tracking, and any member can look at the schedule and see they're being treated fairly. Store a simple `featured` eligibility flag and an optional weight per listing in the portal; the rotation is ~40 lines of pure code. **And every tap goes to Directions, Call, or the listing — never a cart.** I don't want the app skimming a commission off my sales or shipping my customer to some third-party checkout; I want a human walking through my door. Tap-to-call, tap-for-directions (Google/Apple Maps deep link, which you already do in itineraries), done.

**Build vs buy:** Build in-app, deep-link out for the action. There is no product to 'buy' here — this is curation logic and a discovery surface unique to Kingston's geography and to the Chamber's fairness rule, and every reusable piece (banded walk-time listings, open-now, near-me sort, itinerary/hunt loops, map deep links, the ferry slack-time signal) already exists in the codebase. Buying an ad-server or a paid discovery platform would actively work against the epic (it re-introduces auction/pay-to-play) and add cost and complexity for low-tech owners. Integrate only the free, keyless deep links (Directions, tap-to-call). Reserve any 'buy' decision (OpenRouteService/Valhalla isochrones) for a later polish phase, and even then self-host or use the free tier.

**Rough effort:** ~2-3 dev-weeks total, phaseable. Generalizing the restaurant record into a shared Place/listing type + portal editor + up-the-hill discovery surface: ~1-1.5 weeks (the /eat banding and near-me logic are reused, not rewritten). Deterministic fair-rotation featured placement: ~2-3 dev-days (mostly a pure function + a portal 'featured-eligible' flag + admin weighting). 'You have time before your ferry' nudge wired to existing ferry slack-time + a curated quick-loop itinerary/hunt: ~3-4 dev-days. Content backfill (getting up-the-hill shops/galleries/tasting rooms into the new listing type with coords, hours, walk times) is ongoing Chamber/member work, not dev. · **Rough cost:** One-time: developer time only (see effort). Recurring: ~$0 incremental — it runs on the existing ~$31/mo Render hosting (web + Postgres) with no new vendor. Optional isochrones stay $0 on the OpenRouteService free tier or a self-hosted Valhalla container (approximate). The only real ongoing cost is Chamber staff time to keep listings, hours, and the featured-rotation roster current — budget a few hours a season.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing Next.js stack + generalized listing store (DIY) | The whole epic is best built in-app: extend the current Restaurant-shaped record to a general Place/listing type, reuse the /eat band grouping, the near-me distance sort, the hours 'Open now' engine, and the itinerary/hunt 'quick loop' primitive. Rotation is a small pure function. | ~$0 incremental — runs on the existing ~$31/mo Render hosting (web + Postgres) | Strongly preferred. Everything needed already exists in src/lib/types.ts, src/app/eat/page.tsx, src/components/near-me.tsx, src/lib/hours.ts, and the itinerary/hunt stores. No new vendor, no new bill, works offline via ISR + PWA cache. |
| Leaflet + OpenStreetMap (already in use) | Rendering up-the-hill pins with walk-time/open-now popups on the existing map, and optionally a shaded 'up the hill' zone. Already the app's map layer. | Free (OSM tiles; self-attributed) | No change needed — the map, /admin/map geoman editor, and marker categories (shop, drink, art, lodging) already exist. Just point the new listing type at it. |
| OpenRouteService (isochrones/pedestrian) — OPTIONAL, only if true walk polygons wanted | If the Chamber ever wants an accurate 'reachable in 10/15 min on foot' isochrone drawn on the map instead of the current calibrated straight-line estimate. | Free tier ~2,500 requests/day, ~40k/mo (approx, 2026); self-host via Docker for $0 API cost | Overkill for launch — the town is flat and tiny so the existing distance estimate is good enough. Precompute isochrones server-side and cache; do NOT call per-visitor (kills offline + burns quota). Valhalla self-hosted is the free alternative. |
| Google/Apple Maps deep links (already used in itineraries) | The 'Directions' action on every discovery card and loop stop — sends the visitor walking to the door, no cart, no commission. | Free (URL deep links, no API key) | Already the pattern in ItineraryStop.mapQuery. Pair with tel: links for tap-to-call. This is the honest, LTAC-clean 'drives a physical visit' action. |

### Real-world examples

- **Visit Bainbridge Island (the nearby competitor app)** — Category-filtered 'what's near me' discovery, shareable itineraries, and return-ferry alerts on a React PWA — proves the walk-on discovery + itinerary pattern works on this exact ferry-tourist audience one island south. Client-side geolocation only, never persisted (matches the privacy posture here). (https://go.visitbainbridgeisland.org)
- **AllTrails / scavenger-hunt apps (loop primitive)** — GPS-checkpoint 'do this loop, hit these stops' UX — the same mechanic as the app's existing Hunts, repurposed as a timed 'you have 40 min, here's a walking loop' nudge. (https://www.alltrails.com)
- **Main Street America / downtown 'shop local' district apps** — Chamber/BID-curated featured-business rotation and off-the-main-drag wayfinding to spread foot traffic to side-street merchants — precedent for fair, non-auction curation funded by public/association money. (https://mainstreet.org)
- **Washington State Ferries 'Best Times to Travel' + terminal wait data** — The live departure-time / slack-time signal the 'explore beyond the dock' nudge keys off of — already integrated into the app's ferry prediction stack. (https://wsdot.wa.gov/travel/washington-state-ferries)

**LTAC funding:** Cleanly LTAC-fundable: this is a tourism-promotion PLATFORM that surfaces and markets ALL members equally (public money promoting the whole town, not selling ad slots), which is squarely inside RCW 67.28 allowable use. The deliberate design choice of a Chamber-curated fair rotation instead of paid ad-bidding is what keeps it clean — the moment you auction placement or take a per-transaction cut for individual for-profits you'd risk crossing into non-allowable private benefit, which is exactly why this epic routes to directions/call/visit and never a checkout.

**Risks:** Fairness credibility: the rotation must be transparently equal or members will (rightly) accuse the Chamber of playing favorites — make the schedule seeded, deterministic, and viewable so any member can verify their share; avoid any hidden weighting that isn't openly justified (e.g. 'off-strip' or 'open now' boosts should be stated policy, not a black box).; Content gap is the real bottleneck: up-the-hill shops, galleries, and tasting rooms aren't in structured data yet, and many owners are tiny/seasonal/low-tech and won't self-enter. The feature is empty until someone (likely Chamber staff) backfills coords, hours, and walk times — plan for that legwork, not just the code.; Walk-time accuracy and honesty: the calibrated straight-line estimate can be off on real streets/hills; label it 'approx' and never let an over-optimistic '8 min' strand a walk-on who then misses their boat. Tie any 'you have time' nudge to a conservative slack buffer using the real next-departure, not a guess.; Nudge fatigue / mistiming: pushing 'explore beyond the dock' when someone has only 10 minutes, or during off-season quiet, is annoying and erodes trust. Gate it on a generous minimum slack window, on businesses actually open now, and keep it a dismissible suggestion — never a modal that blocks the ferry info people came for.

---

## Reservations, bookings & deposit/card-hold to stop no-shows `P1` · `Cost: High` · 🔒

Let visitors reserve a tasting, table waitlist slot, or kayak tour, with an optional deposit or card hold to end peak-weekend no-shows, capacity caps to spread the post-ferry rush across time blocks, and calendar sync to the tool owners already use. Includes 21+ age-gating for anything alcohol-related.

**Requested by:** Meg Sorensen, Rosa Delgado, Marcus Reed

### Approach

I'm Meg, and I run a small tasting room a two-minute walk from the ferry dock. Every summer Saturday the same thing happens: a boat lands, forty people flood the strip in a twelve-minute burst, and half of them who "reserved" a tasting online never show, so I've held tables and pours for ghosts while walk-ins I could've seated stood in the doorway and left. I need to end no-shows, cap how many people I take per time block, and make anything with alcohol confirm the person is 21+. But I will walk away from any tool that takes a cut of my pours. Rosa (kayak tours) and Marcus (also a tasting room) are with me on this. So here's how I'd build it: **the Chamber app owns the booking EXPERIENCE — the slot picker, the age-gate, the "seats now vs. reserve for later" throttle, the calendar sync — and my own money moves through MY OWN processor, never through a Chamber layer that skims.**

Concretely, extend the business portal I already log into to edit my hours. Add a "Bookings" tab where I define bookable offerings (a tasting, a kayak launch, a table-waitlist slot), set a duration, and — this is the part that saves my Saturdays — set a **cap per time block** (e.g. 6 seats per 30 minutes) so the app can naturally spread the post-ferry flood across the afternoon instead of dumping it all on the 1:15 boat. The public side is a new mobile-first booking flow on my listing: the visitor picks a slot, sees a live "3 seats now / next opening 2:45," does a one-tap **21+ confirmation** for anything alcohol-related (stored as a boolean + timestamp on the booking, gating tasting bookings AND wine-club signups per the acceptance criteria), and confirms. This is net-new — we don't have booking today — but it rides directly on the auth, portal-overlay, and syndication seams that already exist, and it should reuse the same ferry-forecast data so I can show "the 1:15 boat is slammed, grab a 3:00 slot instead."

**For the deposit / card-hold, don't build a payment rail — connect mine.** The cleanest path is Stripe Connect in *Standard/Express* mode where each owner links THEIR OWN Stripe account: the deposit or card-hold (a manual-capture PaymentIntent that authorizes but doesn't charge unless I no-show them) settles straight into my bank, the Chamber platform never touches or holds the money, and Stripe's standard processing fee (~2.9% + 30¢, which I already pay on every card swipe) is the ONLY cost — **zero per-booking commission to the Chamber or the app.** That's the whole point: the platform is free to me, and the only money that moves is between my customer and my bank. For owners too small or too tech-shy to want a card rail at all, the SAME booking form works with NO deposit — just a confirmation and a calendar hold — so nobody's forced into payments.

**Calendar sync is non-negotiable for adoption** — I already live in Google Calendar and I will not babysit a second system. Every confirmed booking, hold, and cancellation must write to my existing Google Calendar. The lowest-lift honest version is a per-offering **iCal feed URL** (the app already publishes iCal feeds for events, so this reuses proven plumbing) that I subscribe to once; the polished version is Google Calendar API two-way OAuth so cancellations disappear from my calendar automatically. Start with the iCal feed to ship, add OAuth write-back as a fast-follow. Offline-friendly matters too: the visitor's booking should queue and confirm even on the spotty dock wifi, and my portal booking list should be readable on my phone behind the bar.

**Build vs buy:** HYBRID: build the booking UI + age-gate + capacity/throttle logic IN-APP, integrate each owner's OWN Stripe for deposits, and offer Square as a deep-link escape hatch for the least-technical owners. Reasoning: the visitor-facing flow (slot picker, '21+ confirm', 'seats now vs. reserve for later', ferry-aware pacing) is the differentiated tourism experience and must live inside the Chamber app on the existing portal/auth/syndication seams — that's also the cleanly LTAC-fundable layer. But do NOT build a payment rail: connecting each owner's Stripe (Connect Standard, manual-capture holds) means the Chamber never touches money, there's zero commission, and liability/chargebacks stay with the merchant. For the scheduling internals (DST, double-booking, per-slot caps), seriously evaluate wrapping Cal.com's open-source engine before hand-rolling — it removes weeks of edge-case risk. And for a couple of owners who'll never adopt anything custom, deep-linking to their free Square Appointments page is a legitimate, zero-build fallback. The one thing to build carefully in-house regardless is the 21+ gate and the calendar sync, because those are the acceptance-criteria non-negotiables that protect liquor licenses and drive adoption.

**Rough effort:** 3-5 dev-weeks for a solid MVP: ~1-1.5 weeks for the portal Bookings tab + public slot-picker + capacity/time-block throttle on the existing stack; ~1 week for Stripe Connect onboarding + manual-capture hold/deposit flow (use the stripe-best-practices skill — Checkout Sessions or PaymentIntents with manual capture); ~0.5 week for the 21+ age-gate + storage; ~0.5-1 week for iCal feed sync (Google Calendar OAuth write-back adds another ~1 week as a fast-follow). Wrapping Cal.com instead of hand-rolling scheduling could trade ~1 week of build for ~0.5 week of integration but adds an ongoing service to run. Plus ongoing per-owner Stripe-onboarding support (low-tech owners will need hand-holding). · **Rough cost:** One-time: ~3-5 dev-weeks of build (the dominant cost; this is the highest-effort epic in the backlog). Recurring platform: ~$0 added — booking data rides the existing overlay stores/DB and hosting stays ~$7-8/mo; iCal + Google Calendar API + Stripe accounts are all free to stand up. Per-owner recurring: only their own card-processing (~2.9%+30¢/txn) on deposits they actually charge, plus optional ~$29-79/mo IF an owner chooses a turnkey buy (Square Plus / Tock) — never billed to the Chamber. Net member cost for the default in-app path = $0 flat, which is the non-negotiable.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Stripe (Connect Standard + manual-capture PaymentIntents) | The deposit / card-hold rail. Each owner links their OWN Stripe account; a manual-capture PaymentIntent authorizes the card (the 'hold') and is only captured on a no-show, or a small deposit is charged up front. Money settles to the owner's bank; the Chamber platform never holds funds. | ~2.9% + 30¢ per transaction (approx, standard US card rate) paid by the owner — the SAME fee they already pay to swipe a card. No platform/application fee if the Chamber sets application_fee_amount to 0. Stripe account is free to open. | HONEST commission note: this is the ONLY money that moves, and it's card-processing, not a per-cover skim. Connect Standard keeps the Chamber off the money entirely (owner is the merchant of record, owns chargeback/liability). Card holds expire in ~7 days — fine for weekend bookings. This is the flat/near-zero-member-cost path Meg demands. |
| Square Appointments (integrate or deep-link, per-owner) | A turnkey fallback for owners who don't want a custom flow: genuinely free tier with online booking, Google Calendar sync, reminders, and deposit collection built in. The app can deep-link owners here or embed their Square booking page. | Free plan = $0/mo (deposits + Google Calendar sync included); no-show fee auto-charging needs the Plus plan ~$29–49/mo (approx). Card processing ~2.6%+15¢ in person / ~3.3%+30¢ online. NO per-booking commission to anyone. | Good escape hatch for the least-technical owners — zero build for the Chamber, and it already does age-agnostic booking. Downside: it's a second dashboard (violates the 'no system to babysit' wish) and the age-gate would have to be a custom intake question. Best as a per-owner opt-in, not the default. |
| Tock (precedent + paid buy option for wineries specifically) | Reservation/deposit system built for wineries and tasting rooms; its 'No-Show Elimination Suite' (free / deposit / prepaid / credit-card-hold) and per-time-slot capacity are exactly this epic. Useful as the gold-standard reference and a buy option for a serious tasting room. | Flat monthly, ZERO per-cover fees — tiers ~$79 to ~$769/mo (approx, 2026). Deposits from ~$5. This flat-fee model is literally the thing Meg is asking for. | Validates that 'flat fee, no skim' is a real market model. Too pricey/heavy to mandate across tiny seasonal members, but a great link-out for a winery that wants best-in-class. Cite it to the LTAC committee as proof the no-commission approach is industry-standard. |
| Google Calendar API (OAuth) + iCal feed (DIY, in-stack) | The calendar-sync requirement. Phase 1: publish a per-offering iCal feed (reuses the app's existing iCal event-feed plumbing) that the owner subscribes to once. Phase 2: Google Calendar API two-way OAuth so cancellations auto-remove from the owner's calendar. | $0 — iCal is static text; Google Calendar API is free within generous quotas. Hosting stays ~$0–8/mo. | Free/DIY option. iCal is one-way (bookings appear, cancellations linger) but ships in days and needs no OAuth consent screen. OAuth write-back is the 'no second system to babysit' finish — build it as a fast-follow once bookings prove out. Both fit the existing Next.js API-route + overlay-store architecture. |
| Cal.com (open-source, self-hostable scheduling engine) | If the Chamber wants a proven booking ENGINE instead of hand-rolling slot/capacity/cal-sync logic. Open-source, handles availability, per-slot caps, buffers, Google/Outlook sync, and Stripe deposits out of the box; can be embedded. | Self-hosted = $0 (own hosting); cloud tiers ~$0–15/user/mo (approx). MIT-licensed core. | Buy-vs-build middle ground: fork the engine, wrap it in Kingston branding + the 21+ gate + ferry-forecast throttle. Saves weeks of scheduling-edge-case work (DST, double-booking, timezone). Risk: it's another service to run and its data model may not marry cleanly to the existing overlay stores — evaluate before committing. |

### Real-world examples

- **Tock (Squarespace-owned) — wineries & tasting rooms** — Flat-monthly, zero-per-cover reservations with a No-Show Elimination Suite: free/deposit/prepaid/credit-card-hold options, deposits from ~$5, and per-time-slot capacity caps. The reference implementation for exactly this epic, and proof the no-commission model is real. (https://www.exploretock.com/join/wineries/)
- **Square Appointments** — Free-tier online booking with real-time Google Calendar sync, automated reminders, deposit collection, and (on paid tiers) automatic no-show/late-cancel fee charging to a card on file — all with no per-booking commission, only standard card processing. (https://squareup.com/us/en/appointments/pricing)
- **Resy / OpenTable 'Resots' & deposit holds (restaurants)** — Credit-card-hold and prepaid-deposit reservations that charge a per-person fee only on no-show, plus capacity pacing across time slots — the mainstream pattern for throttling a rush and killing no-shows. (OpenTable does charge per-cover, so cite it as the pattern to AVOID on pricing, follow on UX.) (https://resy.com)
- **Cal.com (open-source scheduling)** — MIT-licensed booking engine handling availability, per-slot capacity limits, buffers, timezone/DST correctness, Google/Outlook two-way sync, and Stripe deposit collection — a self-hostable core the Chamber could wrap rather than build slot logic from scratch. (https://cal.com)

**LTAC funding:** The PLATFORM layer — booking UI, capacity throttle, 21+ age-gate, calendar sync — is cleanly LTAC-fundable as tourism promotion/visitor-information infrastructure (anchor the pitch to attracting/hosting overnight-and-50-mile tourists, NOT 'more data / local convenience'). The per-transaction deposit/card-hold money is the risky edge: keep public funds OFF it entirely by routing every deposit through the owner's own Stripe account so no lodging-tax dollars ever touch a private for-profit's transaction — get city/county-attorney + LTAC pre-clearance and a written services contract before spending.

**Risks:** Payment liability & PCI: the moment real card holds move, chargebacks, refunds, disputes, and PCI scope appear. Connect Standard (owner = merchant of record) pushes that liability to the owner and keeps the Chamber out of the money — do NOT let the Chamber become the platform-of-record that holds funds, or you inherit both liability and LTAC exposure.; Age-gate is a liability checkbox, not real verification: a one-tap '21+' confirm protects the process but isn't ID verification — pours/pickup still require in-person ID check. Document that clearly so no owner assumes the app cleared them legally, and confirm it satisfies their WA liquor-license terms.; Adoption cliff for low-tech owners: Stripe onboarding (bank account, tax ID, identity verification) is a real hurdle for tiny seasonal members. Without the no-deposit/no-Stripe path AND the Square deep-link escape hatch, most members won't finish setup — and a booking tool nobody lists in is worthless.; Calendar-sync one-way trap: iCal feeds show new bookings but cancellations linger on the owner's calendar, causing double-holds and eroding trust fast. Ship the OAuth two-way write-back before owners depend on it for real capacity, or they'll turn off holds and go back to no-shows.

---

## Ferry-timed order-ahead & pre-order (flat-fee only) `P1` · `Cost: High` · 🔒

Order-and-pay-ahead for coffee/bakery, food truck, and to-go, with the 'ready by' time tied to the real next ferry departure so drinks are hot and nobody misses the boat, and a gentle nudge to opted-in commuters minutes before their boat. Explicitly flat/low per-order fee — never a percentage of the sale.

**Requested by:** Sam Okafor, Marcus Reed, Rosa Delgado

### Approach

Speaking as an owner-operator here: what I actually need is for the app to send me *ready-to-collect* orders timed to the boat, and to take money in a way that doesn't skim a percentage off my razor-thin coffee-and-scone margin. So don't build a payment terminal into the Chamber app. The clean, cheap, defensible move is to make the app a **discovery + ferry-timing + deep-link layer** on top of whatever ordering stack each of us already runs. Most of us are already on Square (Grub Hut, the coffee/bakery folks), and Square's Orders API natively supports a **Pickup fulfillment with a `pickup_at` time** — and charges **0% extra when the customer pays with Square** (or a flat 1% if they pay through another provider), on top of my normal card rate that I'd pay anyway. That's the whole ballgame: the customer taps "Order ahead" in Explore Kingston, lands in *my* Square/Owner.com/Toast online-ordering page with the cart and pickup time pre-filled, pays *me* directly, and the Chamber never becomes a marketplace facilitator or touches a dime of the transaction. Sam's 25–30% marketplace cut is a hard no, and this design makes that cut structurally impossible because the Chamber isn't in the money path at all.

The one genuinely *new* and genuinely *ours* piece — the thing no DoorDash or Square gives us — is the **ferry-timing brain**. The app already ingests live WSF next-departure data and models busyness. Reuse it: when I set up my listing I tell the app my prep time (say "12 min for a latte, 20 for a burrito"), and the app computes a suggested **"ready by" pickup time = next viable ferry departure minus my walk-to-terminal minus a buffer**, and passes that as the `pickup_at` into my ordering page. That's a thin, versioned prediction call — and it's the *exact same* probabilistic board-time seam the line-side-delivery vision already scoped (its seam #1, the `{safeToDeliverUntil, goNoGo}` engine). Build it once here as a **timing advisor**, not a dispatch gate, and the late-2027 delivery concept inherits it for free. Preserve that seam.

For the merchant-not-babysitting-a-screen requirement: I don't want to stare at a tablet mid-rush, and I already don't have to — Square/Toast/Owner.com order alerts print to my existing ticket printer or ping the KDS I already own. The Chamber app should **not** try to become my order-management screen; it should hand the order to the tool I already check. If a member has *no* online ordering at all, the graceful fallback is a **structured pre-order request** (item + name + "ready by" time) delivered via the business portal + an SMS/email nudge, that I confirm and take payment for in person at pickup — dead simple, offline-friendly, zero new hardware, and it still solves the "hot and on-time" problem for the smallest seasonal members.

The commuter nudge is the cheapest high-value bit and it reuses the app's existing privacy-first, opt-in posture. An **opted-in** commuter who's shared a coarse "I usually take the 5:30 from Kingston" preference gets a single gentle push in the ~15-min window before they'd walk to the terminal: "Boat in ~18 min — order a coffee to grab on your way?" That's a scheduled/geofenced notification tied to the ferry data we already have. Keep it strictly opt-in, frequency-capped, and non-persisted per the app's analytics ethos — this is a convenience nudge, not an ad network.

**Build vs buy:** DEEP-LINK OUT for payment and order management; BUILD IN-APP only the discovery UI, the ferry 'ready-by' timing engine, the merchant setup (prep-time + pre-orderable menu), and the opt-in nudge. Do NOT build an in-app checkout. The moment the Chamber app takes payment it becomes a WA marketplace facilitator + PCI merchant-of-record and inherits the exact gift-of-public-funds / picking-winners LTAC exposure the funding note warns about — and it invites the percentage-cut model Sam is vetoing. Buying/integrating a merchant ordering stack (Square/Owner.com/Toast) is the merchant's decision and expense, not the Chamber's. The Chamber's defensible, cheap, ownable value is the ferry-timing brain + discovery layer + nudge, deep-linking each order into the merchant's own zero-/flat-commission rail. This is the same deep-link-directory invariant (seam #3) the line-side-delivery research already locked in.

**Rough effort:** 3–4 dev-weeks for a solid MVP: ~3–5 dev-days for the ferry 'ready-by' timing calc + versioned advisor API (reuses existing WSF data); ~1 week for merchant setup UI in the existing portal (prep-time, pre-orderable menu, pickup point, deep-link config) + Square deep-link/OAuth wiring; ~1 week for the opt-in scheduled/geofenced Web Push nudge (net-new push seam); ~2–3 days for the no-online-ordering SMS/email pre-order fallback. Ongoing: light maintenance + keeping merchant deep-links current. · **Rough cost:** One-time: ~3–4 dev-weeks of build (internal). Recurring to the Chamber: ~$0–8/mo (fits existing hosting; self-hosted VAPID push is free — budget up to ~$0–100/yr only if we opt into a managed push tier). Zero Chamber-side per-transaction cost by design. Merchant-side costs (Square rates, or an optional ~$249–$499/mo flat-fee ordering platform) are borne by each business, not the Chamber and not LTAC.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Square Orders API + Square Online / Checkout links | The merchant's own order-and-pay rail. Native Pickup fulfillment with a pickup_at time is exactly what the ferry-timing engine needs to write. Customer pays the merchant directly; Chamber app just deep-links in with a pre-filled cart + pickup time. | ~2.6–2.9% + ~$0.30/txn card processing (paid by the merchant, as today); ~0% extra Orders-API fee when paid with Square, ~1% if a non-Square processor is used (approx, 2026). No Chamber-side per-txn fee. | Most Kingston coffee/food members are already on Square, so this is the lowest-friction path and self-serve via OAuth. Commission model is honest here: it's the merchant's normal card rate, NOT a marketplace cut. This is the recommended default. |
| Owner.com (flat-rate plan) / Toast Online Ordering — merchant-side, zero-commission | For members who want a fuller branded online-ordering site with genuinely zero per-order commission, to deep-link into from the app. | Owner.com ~$499/mo flat-rate (0% per-order commission) or ~$249/mo + 5% (approx). Toast Online Ordering flat monthly fee, no commission, but POS setup ~$849+ and all-in often ~$1,000+/mo (approx, 2026). | These are MERCHANT expenses, not Chamber/LTAC expenses. Honest commission note: the flat-rate tiers are the point — avoid the percentage tiers (Owner Flex 5%, Cloosiv/Odeko 8–12%) which are exactly what Sam is rejecting. The Chamber never pays these; it only deep-links out. |
| Web Push (VAPID / Web Push Protocol) via the existing Next.js PWA + a scheduler | The opt-in pre-boat 'order ahead?' nudge and 'your order's ready' confirmation, using the PWA's own push — no third-party account needed. | Free (self-hosted VAPID keys). Optional managed layer (e.g. OneSignal free tier up to ~10k subscribers) if we don't want to run our own. | Reuses the app's opt-in, non-persisted privacy posture. The vision note flags the app has NO real-time push/dispatch layer today (~60s ISR) — this epic is the right, low-stakes place to introduce a lightweight scheduled push seam that delivery later reuses. |
| WSF/WSDOT live departure data (already integrated) + existing business portal | The ferry 'ready-by' timing engine and the merchant's self-serve setup (prep-time, menu-of-pre-orderables, pickup-point). Both already exist in the app; this epic extends them. | ~$0 (already built / already hosted). | This is the one piece that's genuinely OURS and the reason a generic ordering app can't replicate it. Build the ready-by calc as a versioned timing advisor so the late-2027 line-side-delivery go/no-go engine inherits it. |

### Real-world examples

- **Starbucks Mobile Order & Pay** — The gold standard for 'set a pickup time, walk in, grab it hot.' Proves the behavior commuters already expect; we're bringing it to independents without the mega-chain app. (https://www.starbucks.com/coffeehouse/mobile-order)
- **Square Online / Square Orders API (Pickup fulfillment)** — Independent cafes and food trucks run order-ahead pickup with a scheduled pickup_at time, paid directly to the merchant at their normal card rate — no marketplace commission. Exactly the rail to deep-link into. (https://developer.squareup.com/docs/orders-api/what-it-does)
- **Owner.com** — Zero-commission, flat-fee branded online ordering built specifically so local restaurants stop paying 15–30% to delivery apps. Real-world proof the flat-fee model Sam wants is viable and adopted. (https://www.owner.com/pricing)
- **BART / transit 'grab-and-go' + WSF terminal context** — Transit-timed retail is a proven pattern; tying an order's ready time to a departure so riders don't miss the boat is the same idea applied to the Edmonds–Kingston sailing schedule the app already tracks. (https://www.wsdot.wa.gov/ferries/schedule/)

**LTAC funding:** LTAC-fundable ONLY for the platform/discovery/ferry-timing/nudge layer, framed as tourism promotion and anchored to the statutory tourist (overnight paid-lodging or 50+ miles), not to local-commuter convenience or 'more visitor data.' Public money must stay OFF any per-transaction fee to a private for-profit; a flat per-order fee, if ever charged, must be pure cost-recovery to the payment processor and never Chamber margin. Get city/county-attorney + LTAC pre-clearance before spending, per the funding note.

**Risks:** Ferry-timing accuracy is the whole promise: a wrong 'ready by' means cold coffee or a missed boat. Vessel swaps, cancellations, and lumpy batch boarding make near-boundary sailings a coin flip — buffer conservatively and add a kill-switch that suspends order-ahead offers on disruptions (mirrors the vision's suspend-line-side-offers seam).; Payment-scope creep: any pressure to add in-app checkout flips the Chamber into a marketplace facilitator + PCI merchant and detonates the LTAC safe path. Hold the deep-link-out line firmly.; Merchant adoption/consistency: tiny seasonal members with no online ordering, mixed tech skill, and no desire to babysit anything. The SMS/email pre-order fallback must be truly dead-simple or they won't use it — and half-populated menus make the feature look broken to commuters.; Nudge fatigue / privacy trust: a poorly frequency-capped or non-opt-in pre-boat push reads as spam and erodes the app's privacy-first reputation. Keep it strictly opt-in, capped, and non-persisted.

---

## Real-time daily status: food-truck location, tap list, food-truck-today `P1` · `Cost: Medium` · 🔒

Let mobile and fast-changing vendors broadcast TODAY's location, hours, and open/sold-out/cancelled status from a phone in seconds, shown on the map where the truck actually is — with auto-expiry so it never displays yesterday's pin. Includes a live tap list and a 'food truck here today' slot for the taproom.

**Requested by:** Marcus Reed, Danny Cho

### Approach

I run a food truck, and here's the honest truth: I decide where I'm parking that morning, sometimes based on weather or whether the ferry's slammed. If your app shows my "home base" pin like it's a fixed restaurant, it's lying to visitors and it makes me look bad when they show up to an empty lot. So don't build me a listing — build me a **"Where am I today?" button**. I open the app on my phone, tap once, it grabs my GPS (or lets me drop a pin on your map if I'm setting up ahead of time), I type today's hours, hit save, and my pin moves to where I actually am. Ten seconds, standing next to my truck. And when I sell out of pork at 1pm, I want ONE tap that flips me to "Sold out" everywhere — your map, the brewery's page, all of it — without calling anyone.

The single most important thing, and the reason I'll actually trust this: **it has to forget me overnight.** A stale pin is worse than no pin — a wrong location is the one thing that'll get me angry reviews. You already solved this exact problem with the ferry boarding-pass override that auto-lapses at midnight Pacific time (`pacificDayString()`). Reuse that dead-simple pattern here: my location/status is stamped with today's Pacific date, and if I don't "check in" again the next day, my pin just disappears from the live map on its own — no timers, no me remembering to turn myself off. That's perfect, because I *will* forget to turn myself off. Default to invisible; make me opt in each day I'm out. That's the behavior that fits a seasonal, weather-dependent vendor who refuses upkeep burden.

This is a **new "daily status" overlay on top of the map, portal, and syndication you already have** — not a new system. My truck is just a new listing type (mobile vendor) in the same schema-driven portal, with a `todayStatus` overlay doc (lat/lng, hours, open/sold-out/cancelled, Pacific-day stamp) living in the same overlay store everything else uses. The map's `map-resolve` filter already drops hidden listings — teach it to also drop any daily-status that isn't stamped today. And because you already syndicate a listing onto other pages, my "here today" status can render on Danny's brewery page automatically (cross-promotion) — the truck-status-on-the-brewery's-listing tie-in in the notes. I'm Danny, by the way — I run the taproom. Give me a **tap list I can edit as fast as Marcus edits his location**: a plain list of what's pouring, one-tap "tapped out" per beer, and a **"food truck today" slot** that shows whichever truck is parked out front — and let the truck fill that slot itself when it checks in near me, so I don't have to chase anyone. Both of us are non-technical and often working with one hand; if it's not doable one-thumb in under 15 seconds, we won't use it, and an unused feature shows stale data, which is the failure mode we're most afraid of.

**Build vs buy:** BUILD in-app, do not buy a SaaS and do not deep-link out. The real-time truck location is genuinely net-new, but every hard part already exists in this codebase: the map, the schema-driven portal, the overlay store, syndication onto other listings, and — critically — the Pacific-day auto-expiry pattern proven in the boarding-pass override, which is the exact mechanism that makes a stale pin impossible. Buying Untappd (~$899–1,199/yr per taproom) or leaning on Street Food Finder both fail the two non-negotiables: they don't give the Chamber control over the auto-expiry rule, and they pull visitors out of Visit Kingston (breaking the cross-promotion where the truck shows on the brewery's page). Keep vendors inside one dead-simple portal they already use. Optionally allow an 'import my Street Food Finder schedule' convenience later, but the source of truth stays in-app.

**Rough effort:** ~4-7 dev-days. Roughly: 1-2 days for the mobile 'Set today' vendor form (geolocation + pin-drop + status toggle) and the todayStatus overlay doc; 1 day extending map-resolve/map rendering to place trucks at live coords and drop non-today statuses; 1 day for the auto-expiry stamp reusing pacificDayString + the sold-out/cancelled one-tap propagation; 1-2 days for the taproom tap list + 'food truck today' slot and the cross-promo render on the brewery listing; ~0.5 day polish/QA. Plus ongoing near-$0 maintenance. · **Rough cost:** One-time: ~4-7 dev-days of build (effectively $0 cash on this personal/Chamber project beyond time). Recurring: ~$0/mo — it runs entirely on the Render + Postgres/Blob stack already paid for (existing ~$31/mo Render hosting for web + Postgres, unchanged). No new SaaS subscription if built in-app; the only way costs rise is if the Chamber chose to buy Untappd (~$899–1,199/yr) instead, which is not recommended.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing business portal + overlay store (in-app, DIY) | The whole feature. Add a 'mobile vendor' listing type and a same-day todayStatus overlay doc (lat/lng, hours, open/sold-out/cancelled, Pacific-day stamp) in the overlay store already backing every portal edit. A one-screen mobile 'Set today' form with a big status toggle. | ~$0 recurring (runs on existing Render/Postgres stack) | Strongly preferred. Reuses the proven boarding-pass auto-expiry (pacificDayString), the Leaflet map, map-resolve filtering, and syndication. No new vendor, no new bill, full control over the auto-expiry rule that makes this trustworthy. |
| Browser Geolocation API (navigator.geolocation) | The 'use my current location' one-tap for the vendor when they're standing at the truck. Falls back to tap-a-pin-on-the-map for setting up ahead of time or when GPS is denied. | Free (built into every phone browser; no SDK) | Privacy-respecting and consistent with the app's opt-in GPS posture: it's a deliberate vendor action, captured only at check-in, not background tracking. Store a coarse-rounded coordinate; never log a location trail. |
| Untappd for Business | Off-the-shelf brewery tap list + digital menu board if Danny wants the full beer-database/rating ecosystem instead of a hand-typed list. | ~$899/yr Essentials, ~$1,199/yr Premium (approx; ~$89.99–$119.99/mo monthly) — API access is Premium-only | Almost certainly overkill and too pricey for a tiny Kingston taproom, and it doesn't do food-truck location at all. Mention as the 'buy' comparison; the in-app tap list should be free to Danny. Don't make the Chamber pay a per-taproom SaaS bill. |
| Street Food Finder / Street Food App | Precedent + optional deep-link-out for trucks that already maintain a schedule there. | Free to consumers; vendor listing/subscription tiers vary (approx) | Honest caveat: these update mostly WEEKLY, not truck-moved-this-morning real-time, and they pull the visitor OUT of Visit Kingston. Use as a reference and maybe an import-your-existing-schedule convenience, not the core. |
| Resend (already in the stack) + PWA web push (roadmap) | Optional 'ping me when a truck checks in near the brewery' or a daily reminder nudging Marcus to set today's status so he actually keeps it current. | Free tier ample at this volume | Nice-to-have, not v1. A gentle 'you haven't set today's location' nudge is the cheapest way to fight the stale-data failure mode. Web push waits for the planned PWA milestone. |

### Real-world examples

- **Street Food Finder** — Nationwide food-truck finder where vendors set locations/schedules and customers see 'trucks near me' on a map — the canonical model, but schedules skew weekly rather than moved-this-morning. (https://streetfoodfinder.com)
- **Street Food App (streetfoodapp.com)** — City-scoped 'today's food truck locations' map used in markets like Portland/Seattle; vendors publish where they'll be that day — closest analog to the 'truck here today' behavior Marcus wants. (https://streetfoodapp.com)
- **Untappd for Business** — The default digital tap-list / menu-board tool for taprooms — verified beer menus, QR/website/printable menus, sold-out toggles — proving the tap-list UX Danny is asking for (just far pricier than Kingston needs). (https://business.untappd.com)
- **Taplist.io** — Lightweight, low-cost digital tap-list display for small breweries (TVs/screens, easy one-tap edits) — the 'simple and cheap' end of the tap-list market and a good UX reference for the in-app list. (https://taplist.io)

**LTAC funding:** LTAC-fundable as visitor information / tourism promotion: it's a free marketing and wayfinding platform telling ferry visitors what's open and where, with no per-transaction fee flowing to any private business — the safe side of RCW 67.28. Keep it a pure information display (location, hours, sold-out, tap list); do NOT add per-order commissions or paid alcohol ordering, which would channel public money to individual for-profits and cross into risky/off-limits territory.

**Risks:** Stale/wrong pins are the whole risk — a wrong location is worse than none. The auto-expiry must be bulletproof (default-invisible, opt-in-daily, Pacific-day-stamped) so a vendor who forgets to check out never shows yesterday's spot. Test the midnight lapse and DST edges the way the boarding-pass override was.; Adoption/upkeep: seasonal one-hand-busy vendors won't update anything that takes more than ~15 seconds. If the 'Set today' flow isn't truly one-thumb, they stop using it and the map goes empty — self-defeating. A gentle daily 'set your location' nudge helps but can annoy; make it opt-in.; GPS accuracy and privacy: browser geolocation can be off by tens of meters or denied entirely, so always offer pin-drop fallback and round coordinates. Capture location only on the deliberate check-in — never a background trail — to stay consistent with the app's privacy-first stance.; Cross-promo consistency: the 'food truck today' slot on the brewery page and the truck's own pin must read from one source of truth, or a truck could show 'here' at the taproom while its own pin says sold-out. Single overlay doc, one status, rendered in both places; define who wins if truck and taproom both edit the slot.

---

## Business-to-business cross-promotion & bundled itineraries `P1` · `Cost: Medium` · _enhances existing_

Let members link their listing to complementary nearby businesses (brewery ↔ food, tasting ↔ dinner, inn ↔ show, truck ↔ host venue) and let the Chamber bundle 'Arts Night', 'stay + do', and pairing loops into single itineraries — so an evening reads as one coordinated downtown experience that keeps visitor spend in Kingston.

**Requested by:** Danny Cho, Meg Sorensen, Priya Nair, Nadia Fischer, Sam Okafor, Marcus Reed

### Approach

**What I want, and what we're building on.** I'm one of the owners who's been saying "we don't coordinate" for years. When a boat lands, a walk-on has maybe two hours before the return sailing, and right now every one of us is shouting "come to MY place" in isolation. Nobody hands the visitor a *plan for the evening*. The good news is the app already has 90% of the plumbing: there's an itinerary/hunts engine, an admin itinerary builder (`/admin/itineraries` over `itinerary-store.ts`, seed in `src/lib/data/itineraries.ts`), a self-service business portal where I edit my own listing, and syndication feeds. This epic is mostly *connecting* things we already have, not building from scratch. The one real limitation to fix first: today an itinerary stop is a free-text blob with a Google-Maps `mapQuery` string (`ItineraryStop` in `src/lib/types.ts`) — it doesn't *point at a real listing*. So a bundled evening can't automatically show my live "open now" hours, my phone, or tonight's event. Step one is to let a stop optionally reference a real business/lodging/event ID, so an itinerary inherits live data instead of going stale the day I change my hours.

**Member-controlled cross-promo (the part I actually touch).** Add a tiny `relatedListings` field to the `Restaurant`/`Lodging` types — an array of `{ id, note }` (e.g. "grab a pint next door before your table"). In the business portal I pick 2–4 complementary neighbors from a dropdown of existing listings and write one line each. Keep it dead simple: no new page, just a "Pairs well with…" section on my edit screen. Reciprocity should be *opt-in and confirmed*, not automatic — when Danny at Friends & Neighbors links to my kitchen, I get a one-tap "show this on my listing too?" prompt. That prevents the awkward case where a place I don't want to be associated with links to me and it auto-appears. On the public listing this renders as a "Nearby & pairs well" chip row that deep-links to the other listing. This is the enhancement the notes call out: low incremental cost on top of the existing listing editor, and it promotes members *to each other's customers* for free.

**Chamber-assembled evenings + food-truck status.** The Chamber's admin builder already assembles ordered stops; upgrade it so stops can be *typed references* (this restaurant → this event → this venue) and add a couple of Chamber-curated templates: "Arts Night" (Firehouse Theater show + a tasting room + a dinner reservation), "Stay + Do" (an inn room + tonight's event + a nearby restaurant), and pairing loops (Friends & Neighbors ↔ a food truck ↔ Los Tres Compadres). Surface these as a single "An evening in Kingston" card on the home page and `/itineraries`, ideally auto-filtered by what's actually open/happening tonight using the existing events calendar and hours data. For the food truck, add a lightweight self-service "out tonight" status: a truck (a business-portal account with no fixed address) sets `tonightVenueId` + a date/time window; that status then renders **on the host venue's listing** ("🚚 Grub-truck out front tonight, 5–8pm") and as an auto-generated event on the calendar. This is the same overlay-store pattern already used for hours and boarding-pass overrides — a same-day flag that lapses at midnight — so it needs zero new infrastructure and no truck owner has to maintain anything beyond one tap.

**Why in-app, cheap, and offline-friendly.** Everything here is content and relationships living in the existing JSON/Postgres overlay stores — no third-party API, no per-transaction anything, so it stays at ~$0 marginal cost and works offline once the PWA caches it. The "bundle" is just a curated itinerary rendered from data we already hold; the "link" is a foreign key I can set myself in 30 seconds. Lodging "stay + do" and "book" actions stay as **deep-links out** to each business's own booking/reservation URL (already modeled as `bookingUrl`/`orderingUrl`) — we send the visitor to the member, we don't broker the transaction.

**Build vs buy:** Build in-app, deep-link out for money. The linking, bundling, and 'out tonight' status are all just relationships over data we already own — extending the existing itinerary and listing stores is dramatically cheaper and simpler than integrating any third-party platform, and it keeps the content offline-cacheable and privacy-clean. Do NOT buy or integrate a food-truck tracking SaaS (overkill for a town with a handful of trucks) and do NOT broker bookings or orders in-app: any 'book this room' or 'order ahead' action should deep-link to the member's own bookingUrl/orderingUrl so we never touch a transaction. The only 'buy' worth considering is the already-planned free GBP/Meta syndication to amplify bundles outward — and that's reuse of existing scope, not new spend.

**Rough effort:** Roughly 2–3 dev-weeks total, and it splits cleanly so it can ship in slices: ~2–4 dev-days to add member-controlled relatedListings + the reciprocal opt-in (highest value, lowest risk); ~3–5 dev-days to upgrade itinerary stops to typed business/event references and add the Chamber 'Arts Night'/'Stay + Do' templates surfaced as an 'evening in Kingston' card; ~3–5 dev-days for the food-truck 'out tonight at [venue]' self-report rendering on the host listing + auto-event. Small ongoing Chamber effort to curate a few seasonal bundles (not dev time). · **Rough cost:** One-time: developer time only (the 2–3 dev-weeks above); ~$0 in new licenses or services. Recurring: ~$0 incremental — it rides the existing ~$7/mo hosting and the free syndication feeds. No per-transaction fees because we deep-link out rather than broker bookings.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing itinerary + overlay stores (in-app / DIY) | The core build. Extend ItineraryStop to carry an optional typed business/event reference; add relatedListings[] to Restaurant/Lodging and a tonightVenueId same-day flag for trucks — all in the existing itinerary-store, business-store, and event-store overlay pattern. | $0 incremental (runs on current Next.js + Postgres/filesystem stack already paid for — existing ~$31/mo Render hosting for web + Postgres) | This is the recommended path for ~everything. Reuses the exact same overlay/tombstone mechanics already shipped for hours and boarding-pass overrides, so it inherits offline caching, admin gating, and the syndication feeds for free. No vendor lock-in, no new bill. |
| Google Business Profile posts / Meta sync (already-planned syndication) | Push a Chamber-assembled 'Arts Night' or a truck's 'out tonight' as a post to members' Google/Meta profiles so the bundle reaches people who never open the app. | Free (GBP API + Meta pilot are free; already scoped in docs/SYNDICATION.md) | Optional amplification layer, not required for the epic. Honest caveat: GBP localPosts are on Google's legacy v4 API and Yelp write is impossible — so treat this as best-effort reach, not a guaranteed channel. |
| StreetFoodFinder / Truckster (paid food-truck platforms) | Reference/precedent for the truck 'where are they tonight' pattern; a truck that already uses one could be deep-linked rather than us rebuilding live GPS tracking. | Free for consumers to browse; vendor plans roughly $10–40/mo approx per truck (truck pays, not the Chamber) | Do NOT integrate as a dependency. Kingston has a handful of trucks, not thousands — a one-tap 'out tonight at [venue]' self-report is simpler and free versus wiring a third-party GPS API. Keep these as a fallback deep-link only. |
| ICS/JSON syndication feeds (existing) + QR posters | Let a host venue embed 'tonight at our place' on its own site via the per-business embed widget, and print a QR on a table tent that opens the 'evening in Kingston' bundle. | $0 (feeds + embed widget already built) | Zero-maintenance win for low-tech owners: the poster is static, the content behind the QR updates itself from the calendar. Reuses the syndication infrastructure already live. |

### Real-world examples

- **Argenta Arts District (North Little Rock, AR) — Third Friday Art Night** — A recurring downtown arts night where visitors grab a passport at their first venue, collect stamps across participating galleries and shops, and turn it in for a drawing on local-restaurant gift cards. Exactly the 'one coordinated evening across many businesses' model, and it maps cleanly onto our existing hunt/passport engine. (https://argentaartsdistrict.org/art-walk/)
- **Downtown Asheville Arts District — First Friday Art Walk** — Monthly self-guided walk where attendees pick up a Passport Card at any participating location, collect five stamps, and win gift cards to local businesses — a Chamber-curated loop that turns independent stops into a single reason to stay downtown all evening. (https://www.downtownavlarts.com/)
- **StreetFoodFinder** — Consumer-facing app with a real-time map of where each food truck is operating right now, plus menus and hours — the reference implementation for 'the truck is out tonight at [venue],' which we replicate as a simple owner self-report instead of a GPS integration. (https://streetfoodfinder.com/)
- **Gallery Walk Lafayette (Downtown Lafayette, IN)** — An organizer explicitly pairs artists with host retail/restaurant businesses for one-night events — a literal precedent for the Chamber brokering 'truck ↔ host venue' and 'tasting ↔ dinner' pairings that both partners then promote. (https://gallerywalklafayette.com/)

**LTAC funding:** Strong LTAC fit: this is tourism-promotion infrastructure that markets the whole downtown as one destination and, by design, promotes all members equally rather than steering public money to any single for-profit — exactly the kind of information/marketing platform lodging-tax dollars are cleanest for under RCW 67.28. It stays firmly on the safe side as long as we do NOT add per-transaction ordering/booking fees or paid 'featured pairing' placements that channel spend to specific businesses; keep bundle inclusion editorial/free and route all purchases out to the member's own site.

**Risks:** Stale/wrong pairings: an owner links to a place that later closes or that they fall out with. Mitigate with the confirmed-reciprocity opt-in, an easy 'unlink,' and rendering live 'open now'/hidden state so a shuttered partner drops out automatically.; Fairness / 'why isn't MY place in the Arts Night bundle' politics — the classic Chamber landmine. Set clear, published criteria for what goes in a Chamber bundle (open that night, category fit) and rotate features seasonally so it doesn't look like favoritism.; Food-truck status decay: a truck sets 'out tonight' and forgets to clear it, so the host listing shows a ghost truck. Use the same same-day-flag-that-lapses-at-midnight pattern already proven for the boarding-pass override — no manual cleanup needed.; Low-tech adoption: tiny owners won't maintain a complex linking UI. Keep it to a single 'Pairs well with…' dropdown + one line of note, one-tap reciprocity, and let the Chamber pre-seed obvious pairs so members opt in rather than build from blank.

---

## Guest concierge / offline guest-guide link for lodging & STR `P1` · `Cost: Medium` · 🔒

A shareable guest-guide link (and printable QR poster) that lodging and short-term-rental hosts paste into check-in messages — covering ferry how-to, busyness, parking, wifi, checkout, last boat, tides, and curated eat/do picks — that works offline at low-signal cabins, deep-links the host's existing Airbnb/Vrbo listing, and exposes a few private host-editable fields. Deflects repeat guest questions with near-zero host upkeep.

**Requested by:** Jordan Blake, Priya Nair

### Approach

Here's what I actually need, in my own words as a host: I run a two-bedroom cabin up off Miller Bay with terrible cell signal, and I answer the same five questions by text every single check-in — "which ferry, where do I park, what's the wifi, when do I check out, when's the last boat back to Edmonds?" I don't want to build or maintain a website. I want the Chamber to give me ONE link and ONE printable QR poster I can paste into my Airbnb and Vrbo check-in message, and then mostly forget about. So please build this as a thin per-unit layer on top of everything you already have, not a new product. Concretely: a stable route like `/g/[unit-slug]` (the "guest guide") that renders a mobile-first page stitched together from content you already own — the ferry how-to card, the live busyness/last-boat info, the parking zones + wayfinding map, curated eat/do picks — PLUS a small private block only my guests see (wifi name + code, my check-in steps, my "the second step is loose, go slow" quirks, and my checkout time). The town stuff (ferry, hours, tides, last boat) stays fresh because YOU maintain it centrally; I only ever touch my handful of fields.

The offline piece is the whole reason this is P1 for me and not a nice-to-have — my guests arrive with zero bars and that's exactly when they're panicking about the return ferry. Since this rides on the PWA/offline milestone that's already P0 on your roadmap (Serwist service worker), please make the guest-guide route a first-class offline target: when a guest opens my link once on the ferry or on my wifi, the service worker should precache that guide's shell + its data (the ferry schedule for their dates, the map tiles for the Kingston core, my private fields, the tide table) into IndexedDB so it still works in the dead zone at the cabin. Cache-first for my guide, with a quiet "showing offline copy, last updated X" banner and network-refresh in the background. This is the one feature where "works with no signal" is load-bearing, so it can't be an afterthought.

For my OTA listing: I know Airbnb has no API and neither of us should pretend otherwise — so just DEEP-LINK out to my existing Airbnb/Vrbo listing (a "See photos, house rules & booking" button that opens my real listing). Do NOT make me re-upload photos or re-type house rules into your system; that's the exact busywork the commercial guidebook apps make me do and why I won't use them. The private fields should be editable right inside the business/lodging portal I already log into — reuse the same `.data` JSON-overlay pattern and the schema-driven editor you built for restaurants, just add a "lodging guest guide" record type. And please honor the acceptance criterion about drift: since I'll only look at this twice a year, if my wifi code or check-in steps haven't been confirmed in, say, 6 months, email me a one-line "confirm your guest-guide fields are still right" nudge (reuse the Resend + the hours "verified on" freshness pattern you already have). That keeps my upkeep near zero without letting my fields silently rot.

Finally the poster: generate the QR server-side pointing at my stable `/g/[unit-slug]` URL, so the printed poster on my fridge NEVER goes stale even if I edit my content or you change the page — the URL is the permanent redirect, no paid dynamic-QR service needed. Give me a one-click "Download printable poster (PDF)" from my portal with the QR, my unit name, and a short "Scan for wifi, ferry & checkout" line, Kingston-branded. That's it. Link + poster + five fields + it works offline. If you nail those four things you'll deflect 80% of my guest texts and, honestly, make me a much better advocate for the app to the other hosts.

**Build vs buy:** Build in-app as a thin per-unit layer, and deep-link OUT to the OTA listing — do not buy a guidebook SaaS and do not integrate an OTA API (Airbnb has none). Reasoning: the expensive 80% of a guidebook (ferry how-to, live busyness/last-boat, parking map, tides, curated eat/do) already exists in this app and is centrally maintained — buying Touch Stay/Hostfully would duplicate all of it in a silo, cost each host money, force photo/rule re-entry, and cut the Chamber out of the content. The only genuinely new code is small: a /g/[unit-slug] route composing existing components, a 'lodging guest guide' record in the existing portal editor (reusing the .data overlay pattern), a static-QR poster generator, and wiring the route into the already-planned Serwist offline precache. Deep-link the OTA listing (a button to the host's real Airbnb/Vrbo page) so hosts never re-enter photos or house rules — that's the friction that kills adoption for low-tech, seasonal owners.

**Rough effort:** Roughly 1-2 dev-weeks for the guide route + portal fields + QR/poster + drift nudge, ASSUMING the P0 PWA/offline (Serwist) milestone ships first. The offline milestone itself is separate, larger foundational work (~1-2 dev-weeks on its own) and is the true gating dependency — most of this epic's risk lives there, not in the guide layer. · **Rough cost:** Effectively $0 additional recurring — all tools are free/open-source and it rides the existing ~$7.25/mo Render hosting; Resend and NOAA are free-tier. One-time cost is just the dev time above. No per-host fee, which is the point.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Serwist (@serwist/next) | The offline engine — the same service-worker toolkit that's already the P0 PWA milestone dependency. Precache the per-guide shell + route, runtime-cache ferry/tide/map data into IndexedDB, cache-first for /g/[slug] so it survives the low-signal cabin. | Free / open-source (MIT). | Next.js-official recommendation for PWAs; generates public/sw.js at build with no hand-written SW code. This epic is explicitly gated on this milestone — build the guide route AS a first-class Serwist precache target rather than bolting offline on later. |
| qrcode (node-qrcode, npm) | Generate the poster QR image server-side pointing at the stable /g/[unit-slug] URL. Runs in the existing Next.js API route, no external call. | Free / open-source. ~1M+ weekly downloads, battle-tested. | Static QR is CORRECT here because our own /g/[slug] route IS the dynamic redirect — the host can edit content freely and the printed poster never breaks. Avoids a paid dynamic-QR subscription (Bitly free tier caps at ~2 dynamic codes/mo; not workable per-unit at scale). |
| React-PDF (@react-pdf/renderer) or a print-CSS HTML page | Render the one-click 'Download printable poster' (QR + unit name + 'Scan for wifi, ferry & checkout' line, Kingston-branded). | Free / open-source. Alternative: a print-stylesheet HTML page the host prints to PDF from the browser = zero new dependency. | For a handful of low-tech owners, a clean print-CSS page is the cheapest and simplest; reach for react-pdf only if you want pixel-perfect PDFs. Reuse public/brand/ assets. |
| Resend (email) | The twice-a-year 'confirm your guest-guide fields are still current' drift nudge when a host's private field goes stale. | Free tier ~3,000 emails/mo, ~100/day — far more than enough for Kingston's lodging count. | Already the chosen email provider in SYNDICATION.md. Reuse the hours 'verified on' freshness timestamp pattern rather than inventing a new staleness system. |
| NOAA CO-OPS Tides & Currents API (station 9445639, Appletree Cove) | The tide table shown in the guide (already an identified data source for the app). | Free public US government API. | Precache the guest's-stay date range of tide predictions into the offline bundle so the tide card works in the dead zone too. Deep-link/attribute NOAA. |

### Real-world examples

- **Touch Stay** — Best-known paid digital-guidebook product for STR hosts — welcome book with wifi, check-in/out, local maps, works offline as a saved web app. Validates guest demand and the offline expectation, but it's a per-host paid silo (~$120-180/yr approx) that makes hosts re-enter everything; our differentiator is Chamber-maintained town content + no re-entry. (https://touchstay.com)
- **Hostfully Digital Guidebook** — Guidebook tied to a PMS with printable options, auto-translation, and (notably) in-guide UPSELLS for early check-in, day trips, transfers. Good feature reference, but the per-transaction upsell model is exactly the LTAC-risky pattern we should avoid for a lodging-tax-funded platform. (https://www.hostfully.com/blog/what-is-a-digital-guidebook/)
- **BetterSTR** — A FREE digital guidebook with QR codes, wifi management, and local recommendations, monetized via wifi-portal marketing. Proves the free-to-host model is viable and that QR-on-the-fridge is the expected distribution — our version is free-to-host too, funded by LTAC instead of ads. (https://betterstr.com/services/guestbook/)
- **Visit Bainbridge Island (competitor tourism PWA)** — The neighboring town's Lovable/Supabase React PWA already does shareable itineraries and return-ferry alerts client-side — direct proof a small-town tourism PWA can serve this audience, and a reason for Kingston lodging to have a first-party equivalent that ties guests to OUR town content. (https://go.visitbainbridgeisland.org)

**LTAC funding:** Clearly LTAC-fundable: it's visitor information/wayfinding (ferry, parking, tides, curated picks) and it directly strengthens the paid-overnight-stay story that lodging owners and the Chamber need for tourism promotion. Keep it on the safe side of RCW 67.28 by having it INFORM only — deep-link out to booking and avoid any per-transaction upsell fees flowing to individual for-profit hosts (the Hostfully upsell model), which is where lodging-tax use gets risky.

**Risks:** Hard dependency on the unshipped P0 PWA/offline milestone — the 'works with no signal at the cabin' criterion is load-bearing and can't be faked; if Serwist offline slips, this epic can't meet its acceptance criteria. Sequence offline first.; Offline data staleness: a cached guide could show a stale ferry schedule or an old last-boat time to a guest with no signal. Must precache the guest's actual stay-date range, stamp a visible 'offline copy, last updated X', and refresh aggressively when signal returns — a wrong last-boat time is worse than none.; Host adoption depends on true near-zero upkeep. If the private-field editor or drift nudge is at all fiddly, seasonal low-tech owners won't touch it and fields rot; keep it to ~5 fields, reuse the existing portal login, and make the drift email a one-click confirm.; Private-field leakage: wifi codes and door/check-in steps are sensitive. The /g/[slug] URL is effectively the access token (fine, matches the OTA message model), but don't index these pages (noindex), don't expose them in the public syndication feeds, and make the slug non-guessable.

---

## Resident / 'Living Here' mode & everyday-services directory `P0` · `Cost: Medium` · 🔒

A genuine resident home in the app — distinct from the visitor experience — with a 'Living Here' relocation section (schools, utilities, medical, community), an honest commute-to-Seattle reality page, neighborhood guides, a visitor-to-resident bridge, resident-serving directory categories (medical, salon, gym, hardware, bank), a light community board, and resident perks. The antidote to a winter-dead, tourist-only app.

**Requested by:** Linda Cho-Ramirez, Dr. Aisha Bello

### Approach

I'm Linda — I run a shop on the main strip, I live three blocks up the hill, and I open this app in December too. Right now it forgets I exist the minute the last summer boat leaves. Here's how I'd build the resident side without you spending a fortune or making me maintain anything hard.

**Reuse the machine you already have; don't build a second app.** The current app is Next.js seed-files → stores → an admin CMS with editable copy blocks and page show/hide. A resident home is a `/living` route section that rides all of that. I do NOT need a new type of business record — extend the existing listing model with resident-serving categories (medical/dental, salon/barber, gym/fitness, hardware, bank/credit-union, vet, auto, childcare) so a hardware store or a dentist is a first-class listing the same way Grub Hut is, editable by that owner in the same portal they already log into. The `/living` "relocation" pages (schools, utilities, medical, worship, community groups) are best as **structured records with a `lastVerified` date and an accountable `owner` field baked into the type** — not free-text prose — because your acceptance criteria demand a verified-date and an owner on each, and because that's exactly the pattern you already used for the ferry-info facts (`ferry-info-store` overlay, admin-editable fields). Copy across the whole section goes in the editable copy registry so the Chamber can fix a phone number in 60 seconds without a deploy. Set the app's home so a resident can flip a persistent "I live here" toggle (a cookie/localStorage preference, privacy-clean, no login required) that re-orders the home page: ferry-commute + community board + resident directory on top, visitor hunts/itineraries demoted. Two homes, one codebase.

**The honest-commute page is your single best asset and it's nearly free — it's the ferry data you already collect.** You already have live WSF schedule + delays + drive-up space, the boarding-pass estimator, and — critically — a self-collected observation log and a busyness forecast that's *season × weekday × hour*. That same model, pointed at the AM-eastbound-off-the-peninsula commute window and Sunday-PM return, IS the "will I make my 6:50 boat" answer. The one honest thing missing is **cancellation/reliability history**: start logging service alerts and missed/cancelled sailings into the same append-only observation store today (it's a tiny addition to what `ferry-observations.ts` already does), so that by the time the page ships it can say "the Kingston run cancelled 6 times last winter, mostly Nov–Jan, mostly the 5:30am." No sugar-coating — that's the whole point, and no resident trusts a commute page that pretends the boat is always on time. Add real drive-alternative context (the Agate Pass bridge / Bainbridge back-route, the Kitsap fast-ferry from the Port with its no-Sundays, $2-over/$13-back reality already in the app). This page alone justifies a resident opening the app on a Tuesday.

**Keep it dead-simple and keep the money clean.** The community board should be *light* — Chamber-moderated post submissions (lost dog, free firewood, garage sale, a broker's open house), not a Craigslist you have to police 24/7; reuse the existing submission/overlay + admin-approval pattern from events. Brokers posting open houses/listings just create calendar+map entries through a lightly-gated portal role — no new surface. Resident perks = a simple "show this screen / show you're local" list of member deals, no payment rails, no commission, nothing to reconcile. And the visitor→resident bridge is almost pure content: a "Loved your visit? Thinking about moving here?" card on delighted-visitor surfaces (end of an itinerary, the webcams page in shoulder season) that deep-links into `/living`. Because this whole section serves LOCALS, wall it off in the budget from day one (see the LTAC note): the Chamber funds resident mode from general/membership dollars, and the app keeps the LTAC-funded tourism layer accounted separately — same codebase, separate cost line.

**Build vs buy:** Build in-app, integrate for facts, deep-link for authority. The resident home, the directory categories, the community board, and the resident toggle should all be BUILT natively — they're routes, one type extension, and content on top of infrastructure the app already has (portal, admin approval, editable copy, page show/hide, verified-date fields). Do not buy a separate relocation product or spin up a microsite: a second surface would fragment the brand, double the maintenance my low-tech members can't do, and break the visitor→resident bridge. The honest-commute page is built from data the app already collects (WSF live + the self-collected forecast/observation log), so it's build, not buy. Where the truth lives with someone else — school enrollment, KPUD water start/stop, Kitsap County sewer/permits (remember Kingston is unincorporated, so it's the COUNTY, not a city) — deep-link out and never mirror; store only a Chamber-owned last-verified date + owner beside each link so it stays honest and re-verification is a scheduled chore. Only 'integrate' where it saves members from re-typing: if the Chamber's membership CMS (GrowthZone/ChamberMaster) holds the member list, seed the resident-serving directory and perks from it rather than hand-curating twice.

**Rough effort:** 3-5 dev-weeks for a solid v1: ~1 week to extend the listing model with resident-serving categories + the resident-home toggle and re-ordering; ~1 week for the structured 'Living Here' relocation records (schools/utilities/medical/worship/community) with lastVerified+owner fields and admin editing; ~0.5-1 week for the honest-commute page (mostly reusing ferry data) plus adding cancellation/reliability logging to the observation store; ~0.5-1 week for the light community board (submission + moderation, reusing the events approval pattern), broker open-house posting, resident perks list, and the visitor→resident bridge cards. Content population (verifying every school/utility/medical fact) is an ongoing Chamber chore, not dev time — but it's the real critical path, so start it in parallel. · **Rough cost:** One-time: ~3-5 dev-weeks of build (the only material cost; internal if Mat builds it). Recurring: ~$0 incremental infrastructure — it rides the existing ~$7.25/mo Render hosting (or the eventual Vercel Pro ~$20/mo the app already contemplates). Cloudflare Turnstile and all the authoritative data sources (WSDOT, KPUD, NKSD, Kitsap County) are free. The one soft recurring cost is human: a Chamber owner spending an hour or two a quarter re-verifying 'Living Here' facts and moderating the community board — budget that as staff/volunteer time, not dollars.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing app stack (Next.js seed files + store overlay seam + admin CMS) | Build the entire resident section natively: new /living routes, resident-serving directory categories extending the current listing model, structured relocation records with lastVerified+owner fields (mirroring the ferry-info-store pattern), editable copy blocks, and the resident-home toggle. No new service. | ~$0 incremental (rides current ~$7.25/mo Render hosting) | Strongly preferred. Everything the epic needs — self-service portal, admin approval flow, page show/hide, verified-date fields, offline-friendly PWA — already exists. This is a content+routes+one-type-extension job, not a platform. |
| WSDOT WSF APIs + the app's own ferry-observation log/forecast (already wired) | Powers the honest-commute reality page: live schedule, per-direction delay, drive-up space, boarding-pass windows, the season×weekday×hour busyness model, and (new) a cancellation/reliability history built from logged service alerts. | Free (WSDOT access code already registered) | The reliability-history piece is a small addition to ferry-observations.ts. WSF publishes no historical ridership/cancellation feed, so the app must keep building its own — start logging alerts now so winter data exists by launch. |
| Authoritative local data sources for the 'Living Here' records (deep-linked, not scraped) | The verified facts behind schools/utilities/medical/community: North Kitsap School District 400 (nkschools.org/enrollment), Kitsap Public Utility District water (kpud.org, 360-779-7656), Kitsap County Public Works sewer/solid-waste, Kitsap Transit, and — because Kingston is UNINCORPORATED — Kitsap County (not a city hall) for permits/services. | Free | Deep-link out to these; do NOT try to mirror their content (it goes stale and you own the error). Each record stores a Chamber-owned lastVerified date + accountable owner so the acceptance criterion is met and re-verification is a scheduled chore, not a rebuild. |
| GrowthZone / ChamberMaster member directory (the Chamber's likely membership CMS) | If the Chamber runs member data here, the resident-serving categories and 'show you're local' perks can seed from the same member list the Chamber already maintains, and its iCal feed (already a Chamber action item to verify) can feed open-house/community events. | Paid — bundled in the Chamber's existing membership-software subscription (~$150+/mo tier, approx; already a sunk cost if in use) | Use only as a data SOURCE to avoid double data-entry for members; keep the resident experience itself in the app. Confirm the iCal feed works (open Chamber to-do) before depending on it. |
| Cloudflare Turnstile (free CAPTCHA) for the community-board submission form | Keeps the light community board / open-house submissions from being spammed without adding login friction or a paid anti-abuse service. | Free | Privacy-friendly, no user tracking — fits the app's privacy-first posture. Pair with the existing admin-approval queue so nothing goes live unmoderated. |

### Real-world examples

- **Homer Chamber of Commerce & Visitor Center (Homer, AK)** — A small, seasonal, water-terminus tourist town (much like Kingston) that runs a genuine 'Relocation / Living Here' section beside its visitor content — schools, utilities, healthcare, cost-of-living — proving a tiny chamber can serve residents and tourists from one site. (https://www.homeralaska.org/local/relocation/)
- **Chewelah Chamber of Commerce (Chewelah, WA)** — A same-state, small-town precedent: a relocation page that walks new residents through library, utility setup, and city services — the exact schools/utilities/community checklist your 'Living Here' acceptance criterion calls for. (https://chewelah.org/relocation/)
- **Asheville.com Newcomer's Guide** — A polished, well-organized newcomer/relocation hub (neighborhood guides, schools, services) that shows how to make resident content genuinely worth opening — a good visual/IA target for the neighborhood-guides and bridge pieces. (https://www.asheville.com/newcomers-guide/)
- **Greater Chambersburg Chamber of Commerce (Chambersburg, PA)** — A 'New Residents' section tied to a community profile + business directory — a model for wiring resident-serving directory categories and a relocation packet into an existing chamber site rather than a standalone microsite. (https://www.chambersburg.org/new-residents/)

**LTAC funding:** Largely NOT LTAC-fundable: resident/relocation content serves LOCALS (economic development / local convenience), which falls outside RCW 67.28's tourism-promotion, tourism-facility, and event-operations allowable uses — funding it with lodging-tax money invites a gift-of-public-funds / wrong-purpose audit finding. Fund resident mode from Chamber general/membership dollars or another grant, and keep it accounted as a separate cost line from the LTAC-funded tourism layer (same codebase, separate books). The lone gray-area candidate is the visitor→resident BRIDGE card, which touches a genuine day-tripper — but I'd still keep it on the non-LTAC ledger to avoid muddying the tourism reporting; the honest-commute page reuses already-collected WSF data, so it costs LTAC nothing regardless.

**Risks:** Content staleness is the existential risk. A relocation page with a wrong school-enrollment rule or an out-of-date KPUD phone number is worse than no page — it erodes trust the whole app runs on. The lastVerified+accountable-owner fields are non-negotiable, and re-verification must be a scheduled, owned chore, not 'someone will get to it.' Deep-link to authorities instead of mirroring so you own fewer facts.; LTAC contamination. If resident work gets paid from lodging-tax money or blurred into the tourism budget, it exposes the Chamber's LTAC funding to an audit finding. Wall it off in the books from day one; the biggest scope decision here is also a bookkeeping decision.; Community board moderation load and liability. Even a light board invites spam, disputes, fair-housing pitfalls on real-estate/open-house posts, and stale listings. Keep everything Chamber-approved before it goes live (reuse the events approval queue), add free CAPTCHA, set post-expiry, and publish clear rules — or it becomes an unpaid part-time job and a liability surface.; The honest-commute page can be TOO honest / go stale. Cancellation history is only credible once a real winter of data exists — shipping it with thin or summer-only data misleads, and Kingston's forecast model already over-predicts busyness on quiet boats (known bias). Start logging alerts/cancellations now, label everything an estimate, and don't ship reliability claims until the sample supports them.

---

## Trusted town-wide alerts & fast cancellation notices `P0` · `Cost: Medium` · 🔒 · _enhances existing_

A single trustworthy, sourced local alert channel for ferry outages, SR-104 closures, power outages, boil-water/emergency info, plus fast owner/manager-fired cancellation alerts (market weather-cancel, kayak-tour wind-cancel, closed-today) that reach people within minutes, notify everyone booked, and clear themselves after the day. Something owners can tell patients, guests, and staff to rely on instead of Facebook rumor.

**Requested by:** Dr. Aisha Bello, Tom Iverson, Marcus Reed, Priya Nair, Hank Boyd

### Approach

Build this as two layers on top of what we already have, not one big new system. **Layer 1 — the sourced "town status" feed — is mostly an aggregation job over official APIs we're already entitled to.** The ferry piece is done: `wsf.ts` already pulls `/schedule/alerts` and we surface service alerts + boarding-pass banners. Add SR-104 closures from the **WSDOT Highway Alerts API** (same Traveler Information API family and the *same* access-code we already registered as `WSDOT_API_KEY` — no new signup), filtered to SR-104 and high/highest impact. Add weather/storm/wind/flood advisories from the **free, keyless NWS `api.weather.gov/alerts/active` feed** scoped to our known SEW forecast zone. Each item renders with its **source name + official link + fetch timestamp** ("via WSDOT · 8:42 AM"). The hard rule the acceptance criteria demand — *never invent a status* — falls out of the architecture if we make the feed strictly a pass-through: if an upstream fetch fails or returns nothing, the card says "No ferry alerts — checked 8:42 AM" or "Status unavailable," never a fabricated "all clear." This is a new `/alerts` page plus a compact banner slot on the home live-strip, reusing the ISR + fallback-snapshot pattern the ferry status already uses. **Power outages are the honest gap:** there is no clean first-party Kitsap PUD/PSE API. Rather than scrape poweroutage.us and imply authority, deep-link out to the PSE outage map and label it "not live in-app," which is exactly the "honest about missing data" behavior the criteria want.

**Layer 2 — owner-fired cancellations — reuses the business/nonprofit portal, not a new login.** A market manager, kayak-tour operator, or restaurant owner already has a portal account editing their listing/hours/events. Add a single "🚫 Post a cancellation" action: pick a reason chip (Weather cancel / Closed today / Short hours), optional one line, tap send. It writes a short-lived alert into the same overlay store the portals already use, **scoped to the Pacific day and auto-expiring at local midnight** — we already solved that exact "clears itself after today, no timer/DST math" problem for the boarding-pass override (`pacificDayString()` in `wsf.ts`), so copy that mechanism. The alert appears immediately on that business's listing card, on `/alerts`, and in its syndication feed. This is a couple taps, mobile-first, and needs zero maintenance because it self-destructs.

**The "reaches people in minutes / notifies everyone who booked" part is genuinely gated on web push**, which the roadmap already parks in the PWA milestone (VAPID keys are free; iOS only delivers push to an installed PWA). Until push ships, be honest about the delivery channel: the fired alert reaches anyone with the app open right now (the in-page `Notification` pattern we already ship for ferry reminders), plus an **`.ics`/email path for the "everyone who booked" case** — if tour bookings live anywhere with contact info, fire a templated email via Resend (already in our syndication plan) so a wind-cancel doesn't depend on the customer happening to have the app foregrounded. I'd sequence it: ship the sourced feed + fired-cancellation-with-in-page-notify now (high value, cheap, clean LTAC), and light up true push the moment the PWA manifest/service-worker milestone lands, because that manifest is a shared dependency for ferry reminders phase 2 anyway.

**Build vs buy:** Mostly BUILD in-app by aggregating official feeds, with two deliberate deep-links-out. Build the sourced town-status feed and the owner-fired cancellation flow ourselves: both are thin layers over infrastructure we already run (WSDOT auth, ISR+fallback, portal auth, day-scoped overlay stores, in-page Notification), so buying a SaaS alert product would add cost, a login, and a privacy/tracking footprint for capability we already have 80% of. INTEGRATE the two upstream sources (WSDOT Highway Alerts, NWS) as pass-through adapters — never restate their status in our own words. DEEP-LINK OUT for the two things we can't source honestly or shouldn't own: power outages (no first-party API → PSE map) and true resident 911-grade emergencies (that's Kitsap DEM's CodeRED/CAP system → link the official sign-up). For the delivery mechanism, build push in-house with the free web-push lib once the PWA milestone lands rather than adopting a push SaaS, to preserve the privacy-first, no-third-party-tracking posture.

**Rough effort:** Sourced feed (WSDOT SR-104 + NWS adapters, /alerts page, home banner, source/timestamp/unavailable states): ~3-5 dev-days, since it reuses the ferry adapter/ISR/fallback pattern. Owner-fired cancellation flow (portal action, day-scoped auto-expiring overlay, listing + syndication surfacing, in-page notify): ~3-4 dev-days. Web-push delivery for 'reaches people in minutes' + booked-customer notify: ~1-1.5 dev-weeks but BLOCKED on the PWA manifest/service-worker milestone (shared with ferry reminders phase 2) — schedule with that, not this. Ongoing: minimal (feeds self-refresh, cancellations self-clear); light seasonal QA that SR-104/NWS payloads haven't changed shape. · **Rough cost:** One-time: dev effort only (~1.5-2.5 dev-weeks total across both phases). Recurring: ~$0 net new — WSDOT and NWS are free, cancellation flow reuses existing hosting (~$7.25/mo Render already spent), VAPID/web-push are free. Only potential add-ons: Resend paid tier (~$20/mo) IF booked-customer email volume outgrows the free tier, and OneSignal (free tier) only if we choose not to self-host push. Realistically $0-20/mo on top of current hosting.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| WSDOT Traveler Information API — Highway Alerts | Official SR-104 closure/incident/maintenance alerts (open/closed status, impact level, start/end location) for the town-status feed. | Free. Uses the same access-code we already registered for the ferry API (WSDOT_API_KEY) — no new account. | REST at HighwayAlertsREST.svc; filter by RoadName='104' and Priority=Highest/High so we don't spam minor cones. Same auth + fetch/fallback pattern as our existing WSF calls, so it slots straight into wsf.ts-style adapters. |
| NWS / api.weather.gov alerts (alerts/active) | Storm, high-wind, flood, and advisory alerts (the 'kayak-tour wind-cancel' context and general emergency weather) scoped to our SEW forecast zone. | Free, no API key, no account. CAP v1.2 / JSON-LD; rate-limit ~1 req / 30s (fine with ISR caching). | We already know the SEW gridpoint (SEW/121,78) and tide station. Requires a descriptive User-Agent header per NWS policy. Cache 5–10 min. Note: NWS does NOT issue boil-water notices — those come from the water utility/health district, so that class stays a deep-link/manual-post, not an auto-feed. |
| Existing business/nonprofit portal + day-scoped overlay store (in-house) | The owner-fired cancellation flow: two-tap 'CANCELLED today / short hours' that self-clears at Pacific midnight and shows on the listing, /alerts, and syndication. | $0 — reuses code we already run (portal auth, overlay stores, pacificDayString() day-scoping copied from the boarding-pass override). | No new dependency. The auto-expire-at-midnight logic is already proven; the fired alert is just another day-scoped overlay doc. This is the cheapest, lowest-maintenance path and keeps owners in one login. |
| web-push (npm) + free VAPID keys, delivered via our own cron/route | True 'reaches people within minutes' + 'notify everyone booked' when the app is closed — the deferred delivery mechanism. | Free (self-hosted). VAPID keys are free to generate; browser push services (FCM/Apple/Mozilla) are free. Only cost is the PWA-milestone dev work. | Gated on the PWA manifest+service worker (shared dependency with ferry reminders phase 2). iOS delivers push ONLY to an installed PWA (Add to Home Screen). If we'd rather not self-host subscription plumbing, OneSignal free tier is a drop-in alternative but adds a third party and a tracking-SDK privacy tradeoff we've been avoiding. |
| Resend (transactional email) | The 'one tap instantly notifies everyone who booked' path for tour/night cancellations, independent of whether the customer has the app open. | ~Free tier ~3,000 emails/mo / 100/day (approx); paid ~$20/mo for higher volume. | Already named in our syndication plan (docs/SYNDICATION.md). Template a wind-cancel/closed-today email keyed off whatever booking contact list exists. Email is the reliable fallback until push is universal, especially for the booked-customer audience. |

### Real-world examples

- **WSDOT — official ferry & highway alerts (the authority we cite)** — Publishes real-time WSF service alerts and SR-104 highway alerts via a free developer API and a public travel-alerts page; the gold standard for 'sourced, timestamped, don't invent it.' We surface their data with attribution rather than restating it. (https://wsdot.wa.gov/travel/real-time/alerts)
- **National Weather Service — api.weather.gov** — Free, keyless, government-authoritative alert feed (CAP) queryable by forecast zone; the model for a trustworthy weather/emergency source that we pass through with a source badge and 'unavailable' honesty when a fetch fails. (https://www.weather.gov/documentation/services-web-alerts)
- **Puget Sound Energy — outage map** — The real Kingston-area power authority. No clean public API, so the honest pattern is to deep-link their live map and label power as 'not live in-app' rather than scrape an aggregator and imply authority — exactly the 'honest about missing data' criterion. (https://www.pse.com/en/outage/outage-map)
- **Genasys/CodeRED-style county emergency alerting (Kitsap DEM)** — Shows the resident-safety alerting job is legally the county's, delivered via opt-in SMS/CAP. Our app should link residents to the official county sign-up for true emergencies, not position itself as the 911-grade channel — which keeps us on the right side of the LTAC visitor-info framing. (https://www.kitsapdem.org)

**LTAC funding:** The visitor-facing sourced feed (ferry/SR-104/weather safety + travel info) and the platform that powers owner cancellations are clean LTAC — this is tourism information/promotion infrastructure and visitor safety, squarely fundable. The caveat is scope-creep into resident 911-grade emergency alerting (boil-water, evacuation), which serves residents and belongs to the county; keep that as a deep-link to Kitsap DEM so the app's LTAC-funded role stays 'visitor information,' not 'municipal emergency system.'

**Risks:** Delivery-mechanism gap: 'reaches people within minutes' is only half-true until web push ships, and iOS push requires the visitor to have installed the PWA — so early on the fired-cancellation reach is limited to app-open users + the email fallback. Set owner expectations honestly and prioritize the PWA milestone.; Trust/liability from a wrong or stale status: if we ever show 'all clear' when SR-104 is actually shut, we've done real harm and lost the trust that's the whole point. Enforce strict pass-through + explicit 'unavailable'/'checked at' states, and never synthesize a status the source didn't assert.; Sourcing coverage holes: power outages have no first-party API, and boil-water/health notices come from the water utility, not NWS or WSDOT — so parts of the 'emergency info' promise are deep-link-only, not live. Be explicit in the UI about what's live vs linked so the feed isn't perceived as complete when it isn't.; Owner misuse / stale alerts: an owner could fire a cancellation and forget it, or use it as ad space. Day-scoped auto-expiry handles the 'forgot to clear it' case; add a light rate-limit and a one-line 'this clears at midnight' confirmation so the channel stays trustworthy and doesn't become Facebook-rumor 2.0.

---

## Owner analytics, loyalty & LTAC / grant impact reporting `P0` · `Cost: Medium` · 🔒 · _enhances existing_

Two linked needs: (1) plain-English, privacy-first per-listing analytics owners will actually read — views, direction taps, tap-to-call, waitlist/RSVP, ideally correlated to ferry-arrival times and local-vs-visitor split — plus simple digital loyalty/punch cards and resident perks; and (2) defensible, exportable overnight-stay / 50-mile-visitor and attendance/volunteer-hour data the Chamber drops into LTAC and arts-grant reports.

**Requested by:** Priya Nair, Danny Cho, Nadia Fischer, Carol Whitfield, Rosa Delgado, Meg Sorensen, Sam Okafor, Marcus Reed, Jordan Blake, Linda Cho-Ramirez

### Approach

Look, I run a small place on the main strip and I do not have time to learn Google Analytics, and honestly I do not want the Chamber hoarding my customers' data either. So what I actually need is two clean things built on top of what the app already collects, not a new tracking empire. The app already logs page views, coarse IP-geo, outbound clicks, and opt-in block-rounded GPS. **Enhance that, do not replace it.** The privacy-first event pipeline is already there and the Bainbridge teardown validated the posture — the work is turning those raw events into (a) a monthly summary I'll actually read and (b) a grant export the Chamber hands to LTAC. Keep it in-house on the existing Next.js stack; the moment you route my analytics through a third party you've broken the privacy promise that lets this whole thing be publicly funded.

For **my dashboard**, give me one screen per listing: views, "get directions" taps, tap-to-call, and waitlist/RSVP counts for the month, with last month next to it so I can see up or down. Plain English, no bounce-rate jargon. The clever part — and the part I'd brag about — is the **ferry-time correlation**: you already have the WSF arrival feed and the busyness forecast, so bucket my engagement into the ~12-minute windows after a boat lands versus dead time. That tells me when the walk-on wave actually looks me up, which is worth more to me than any pageview total. For **local-vs-visitor split**, do it the honest, coarse way: infer "visitor" from IP-geo region and, only for people who opted into GPS, whether their rounded home block is 50+ miles out — never per-person, always a rough percentage, and **turn all of it OFF around the clinic listing** (no location signal near health-adjacent stuff, no login gate on alerts). Roll up ties to the statutory tourist test but present it as a signal, not a headcount.

The **LTAC/grant export** is the non-negotiable part, because it's what keeps the funding — and therefore the app — alive. The Chamber needs a one-click, pick-a-date-range export (CSV + a clean PDF summary) that pulls attendance/RSVP from events, ticketing where we have it, volunteer-hours from the nonprofit portal shifts, plus the two statutory signals: (1) **overnight** — sourced from the lodging listings' booking/RSVP data and an optional "where are you staying tonight?" one-tap prompt, and (2) **50-mile** — from the coarse geo split. Label every number with its method and confidence so a grant reviewer sees an honest estimate, not a fake precise count. That defensibility is the whole point: RCW 67.28.1816 literally asks for people "staying overnight in paid accommodations" or traveling "fifty miles or more one way," so we should mirror that language on the export.

For **loyalty and resident perks**, keep it dead simple and paperless. A digital punch card that lives as an Apple/Google Wallet pass or a plain web link — customer scans a QR at my counter, no app download, no account. Ship the DIY version inside the app (a signed QR + a stamp counter in the existing portal) so members pay nothing, and treat a paid platform as the fallback only if the wallet-pass plumbing gets ugly. A "resident perk" is just a loyalty card flagged as resident-verified (rough, honor-system + coarse-geo, not a surveillance check). This drives repeat visits AND quietly feeds attendance/return-visit numbers into the same grant export — one build, two payoffs.

**Build vs buy:** Build in-app for the analytics, the LTAC export, and the basic loyalty card; deep-link/integrate a paid platform only as a loyalty fallback. The analytics and grant-export layer MUST be built (or self-hosted) in-house — routing it through a SaaS breaks the first-party, no-hoarding privacy promise that is the legal basis for LTAC funding, and no off-the-shelf tool does ferry-time correlation or a statutory 50-mile/overnight export anyway. The event data already exists in the app, so this is a rollup-plus-UI job, not a new system. For loyalty, build the DIY Wallet-pass punch card in-app so members pay nothing across the whole town; reserve a per-business tool like Loopy Loyalty for the few members who want heavy automation, since its per-location pricing doesn't scale to 166 listings.

**Rough effort:** Owner dashboards + monthly rollups: ~1-2 dev-weeks. Ferry-time correlation + local-vs-visitor coarse split (with clinic exclusions): ~1 dev-week. LTAC/grant export (CSV + PDF, date-range, method/confidence labels, volunteer-hours + overnight/50-mile signals): ~1-1.5 dev-weeks. DIY Wallet-pass punch card + resident perk flag: ~1-1.5 dev-weeks. Total ~4-6 dev-weeks, phased; plus ongoing light maintenance and a yearly review of the export against current LTAC/JLARC report format. · **Rough cost:** One-time: ~4-6 dev-weeks of build. Recurring: near $0 if analytics stay in-house on existing hosting (maybe a small storage bump for rollups; free GeoLite2). If a self-hosted substrate (Umami) is added, still ~$0-20/mo. Loyalty fallback (Loopy et al.) only if used, ~$25-95/mo per participating business — approx — so keep it opt-in, not chamber-wide.

### Tools / services

| Tool | For | ~Pricing | Notes |
|---|---|---|---|
| Existing in-house event pipeline on Next.js + Postgres (enhance, don't add) | Per-owner dashboards, ferry-time bucketing, local-vs-visitor split, and the grant export are all just queries/rollups over the events the app already captures (views, direction taps, tap-to-call, RSVP, coarse IP-geo, opt-in rounded GPS). Add materialized monthly rollup tables + a couple of chart components in the portal. | ~$0 marginal (already on ~$0-8/mo hosting; maybe a small bump for rollup storage) | Keeps data first-party and privacy-controllable, which is exactly what makes it LTAC-defensible. No per-customer profiles, no third-party pixel. |
| Umami (self-hosted, MIT) — optional analytics substrate | If the team wants a battle-tested, cookie-free events store instead of hand-rolling rollups. Multi-site from one install, each listing gets its own site ID, has email summaries. Could back the raw layer while custom UI sits on top. | Self-host: free (MIT, fits ~2GB). Umami Cloud: free Hobby tier 100k events/mo, Pro ~$20/mo for 1M events/20 sites — approx. | Cookie-free and privacy-first by design. Use only as the collection/storage layer; owners still need the plain-English + ferry-correlation UI on top, which Umami won't give you. |
| Plausible (self-hosted CE or cloud) — alternative substrate | Same role as Umami if you prefer its aggregated multi-site dashboard. Cookie-free, GDPR-friendly. | Cloud ~$9/mo (10k pv) up to ~$90/mo (1M pv) — approx; self-host free but needs ~4GB (ClickHouse). | Heavier to self-host than Umami. Only worth it if you're not already storing events in Postgres. Approx pricing. |
| Loopy Loyalty (PassKit) — paid loyalty fallback | Apple/Google Wallet digital stamp cards with zero customer app download if the in-app DIY wallet-pass build proves fiddly. | ~$25/mo Starter (1 card/location) up to ~$95/mo Ultimate (10 cards) — approx; 15-day trial. Per-member cost adds up fast across 166 businesses. | Per-business pricing does NOT scale to a whole-town membership cheaply. Prefer the DIY in-app punch card; reserve Loopy for the handful of members who want richer automation. FaveCard/Loop.fans have free tiers worth a look too. |
| WSF vessel/terminal data (already integrated) + coarse IP-geo lib | Ferry arrival windows to bucket engagement; MaxMind GeoLite2 or an equivalent free IP-region lookup to derive the visitor split at region granularity only. | WSF feed already in the app ($0). GeoLite2 free with license; commercial IP-geo ~$0-50/mo if higher accuracy needed — approx. | Region-level only — never store full IP or street-level location. This is the honesty line that keeps the funding clean. |

### Real-world examples

- **Plausible Analytics** — Open-source, cookie-free, privacy-first web analytics with a clean multi-site owner dashboard and no personal-data collection — the model for 'analytics without surveillance' and the posture that makes public funding defensible. (https://plausible.io)
- **Umami** — MIT-licensed self-hosted analytics; one install serves many sites each with its own ID and email summaries — a realistic zero-cost substrate for per-listing owner reports. (https://umami.is)
- **Loopy Loyalty (PassKit)** — Digital stamp/punch cards delivered straight to Apple & Google Wallet with no app download and no customer account — the exact frictionless loyalty UX low-tech members and one-time ferry visitors will actually use. (https://loopyloyalty.com)
- **WA JLARC Lodging Tax reporting (RCW 67.28.1816)** — The statewide lodging-tax reporting regime every LTAC recipient files into — it explicitly asks for estimated travelers staying overnight in paid accommodations or traveling 50+ miles one-way, which is the exact schema our grant export should mirror. (https://leg.wa.gov/jlarc)

**LTAC funding:** Strongly LTAC-fundable: this is the tourism-promotion PLATFORM plus the exact overnight-stay and 50-mile visitor reporting RCW 67.28.1816 requires recipients to produce — arguably the most defensible spend in the whole app, since it's what documents the funding's impact. Stay clear of anything that skims a per-transaction fee to a private for-profit (e.g. paid loyalty redemptions or ordering commissions); the analytics/reporting/loyalty tooling itself is fine, but revenue channeled to individual businesses is the risky line.

**Risks:** Garbage-in on the statutory signals: the 50-mile and overnight numbers are inherently estimates from coarse geo and opt-in prompts. If we present them as precise counts a grant reviewer will (rightly) distrust them — every figure must be labeled with method + confidence, or we jeopardize the funding we're trying to defend.; Privacy overreach / clinic exposure: any drift toward per-person tracking, full IP storage, or location signals near the health-adjacent clinic listing breaks the privacy posture that makes the money legal and will burn community trust. Hard-code the clinic exclusion and the no-login-for-alerts rule.; Low-tech owner drop-off: if the dashboard reads like Google Analytics, owners won't open it and the 'they'll actually read it' acceptance criterion fails. Ruthlessly plain-English, two-minute glance, email the summary so they don't even have to log in.; Loyalty cost/scale creep: per-business SaaS loyalty (Loopy/Stamp Me) doesn't scale to 166 members and risks pushing us toward commission models that are LTAC-off-limits. If the DIY Wallet-pass build slips, the fallback must stay opt-in and non-commissioned, not chamber-wide.
